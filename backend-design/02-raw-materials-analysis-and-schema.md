# المواد الخام — تحليل المتطلبات وتصميم البيانات

> نسخة تاريخية: المرجع الحالي هو [الوثيقة الموحدة للموردين والمواد الخام والتحذيرات — MERN](./00-unified-mern-spec.md). تصميم PostgreSQL/UUID في هذه النسخة استُبدل بتصميم MongoDB/Mongoose في الوثيقة الموحدة.

الإصدار: 0.2 — توحيد السحب النهائي والتالف، وجعل انتهاء الصلاحية تنبيهًا لا يمنع البيع.

المشروع: باك إند جديد مستقل لإدارة الكافيه. هذه وثيقة تصميم للمراجعة وليست تنفيذًا أو Migration. مصدرها شرح المستخدم ومراجعة كود الفرونت، وليست مستمدة من الباك الحالي.

وثيقة مرتبطة: [تحليل الموردين والـSchema](./01-suppliers-analysis-and-schema.md).

## 1. حالة القرارات

- **متفق عليه:** ما طلبه المستخدم صراحة.
- **مقترح:** استكمال منطقي للحالات والقواعد، قابل للتعديل قبل التنفيذ.
- **مؤجل:** يحتاج تحليل المشتريات أو المنتجات أو الطلبات أو التقارير.

أنواع البيانات بصياغة PostgreSQL وUUID مقترحة للاتساق مع وثيقة الموردين؛ اختيار التقنية النهائي لم يُحسم. لا تعد هذه الوثيقة موافقة ضمنية على تنفيذ الكود.

## 2. الهدف والنطاق المتفق عليه

1. صفحة لتعريف المواد الخام، وتحتها جدول يعرض بياناتها.
2. لكل مادة صفحة تفاصيل تعرض بياناتها والدفعات المسجّلة عليها.
3. إجراءات الجدول: تعديل، حذف، سحب. فتح التفاصيل من اسم المادة مقترح لتجنب إضافة إجراء رابع.
4. وحدتان للمادة: كبيرة وصغيرة، مع التحويل بينهما.
5. المستخدم يدخل الكمية بالكبيرة، وبجوارها كمية الصغيرة محسوبة تلقائيًا للقراءة فقط.
6. تسجيل السعر واستخدام التحويل لحساب تكلفة الصغيرة لاحقًا.
7. إضافة الكميات والدفعات من قسم المشتريات فقط؛ كل كمية توريد جديدة دفعة جديدة.
8. كل دفعة لها كميتها وسعرها وتواريخها المستقلة مثل المعلومات الموجودة بالفرونت.
9. أولوية قابلة للتغيير تحدد ترتيب الخصم من الدفعات أثناء البيع.
10. وظيفة «سحب كمية» من دفعة محددة عند تلف كمية أو فقدها، مع إنقاص المخزون وقيمته.
11. قسم «المواد المسحوبة» يحفظ سجل السحب كاملًا.
12. سحب التالف هو نفسه السحب النهائي: عملية واحدة وسجل واحد ونوع حركة واحد WITHDRAWAL، بلا تشغيل أو استعادة للكمية المسحوبة.
13. تواريخ لكل الإنشاءات والتغييرات والحركات، وفق توجيه المستخدم العام.
14. انتهاء الصلاحية لا يمنع البيع ولا يستبعد الدفعة من ترتيب الخصم؛ التاريخ والتنبيه للمعلومة فقط.

تفسير يحتاج بقاءه ظاهرًا للمراجعة: فُهم «مش عايز تحديث» على أنه إزالة زر Refresh اليدوي، مع تحديث البيانات تلقائيًا بعد الحفظ. ليس المقصود إلغاء وظيفة تعديل بيانات المادة التي طلبها المستخدم صراحة.

## 3. مراجعة الفرونت والفروق المطلوبة

### 3.1 نموذج التسجيل الحالي

في `AddMaterialForm.jsx` توجد: اسم المادة، وحدة واحدة، كمية افتتاحية، سعر الوحدة، المورد، تاريخ الإضافة، تاريخ الصلاحية الاختياري، الحد الأدنى للمخزون، والتنبيه قبل الصلاحية بالأيام.

الوحدات في `inventoryConstants.js` هي كيلو جرام ولتر وقطعة. هذه القائمة الحالية لا تكفي للوحدتين ومعامل التحويل والكرتونة والعبوة.

التعديل المطلوب في التصميم:

- استبدال الوحدة الواحدة بزوج وحدات ومعامل تحويل.
- فصل تعريف المادة عن استقبال كمية: النموذج يعرّف المادة برصيد صفر، لأن كل كمية يجب أن تأتي من المشتريات فقط.
- نقل الكمية وسعر التوريد وتاريخ استلامه وصلاحيته إلى دفعة المشتريات.
- الاحتفاظ بسعر مبدئي للوحدة الكبيرة في تعريف المادة كمقترح، مع تسميته «سعر مرجعي»؛ ليس تكلفة فعلية لمخزون لم يُشترَ.
- تاريخ إنشاء تعريف المادة تلقائي، وهو مختلف عن تاريخ استلام الدفعة.

هذا الفصل يستند إلى توجيه المستخدم الأحدث بشأن حصر إضافة الكميات في المشتريات، ويحل تعارضه مع وجود كمية افتتاحية في النموذج القديم. المخزون القائم عند بدء التشغيل يحتاج مسارًا من قسم المشتريات نحدده في تحليله؛ لا نُنشئ فاتورة وهمية أو دينًا على المورد تلقائيًا لمعالجته.

### 3.2 جدول المواد الحالي

يعرض الاسم والوحدة والكمية وآخر سعر والمورد وأقرب صلاحية وحد التنبيه وتنبيه الصلاحية، مع العرض والحذف والبحث والتحديث وأحجام صفحات 10/25/50.

حسابات الواجهة الحالية تعتمد على مصفوفة الدفعات المحمّلة: مجموع كمياتها، آخر سعر حسب تاريخ الإضافة، وأقرب تاريخ صلاحية دون استبعاد الدفعات الفارغة. في التصميم الجديد تأتي الملخصات من الخادم لكل الدفعات، ولا نعيد حسابها من صفحة جزئية.

### 3.3 تفاصيل المادة الحالية

في `MaterialDetailsPage.jsx` توجد البيانات العامة ونموذج إضافة دفعة وجدول دفعات وأولوية وسحب يدوي.

أعمدة الدفعات الحالية: الرقم، الافتتاحية، المتاحة، المستهلكة، السعر، الإضافة، الصلاحية، الأولوية، الحالة، والإجراءات. وتسمح الواجهة بإضافة دفعة وتعديل الكمية والسعر والتاريخ وحذفها مباشرة.

التصميم الجديد يحذف إضافة الدفعات المباشرة من صفحة المادة، ولا يسمح بتعديل كمية دفعة أو سعرها بعد الترحيل مباشرة. تصحيح مستند شراء له مساره في المشتريات. أولوية البيع وحدها قابلة للتعديل هنا، مع سحب تالف موثّق.

الحساب الحالي «الافتتاحية − المتبقية = المستهلكة» يخلط البيع بالتالف. المطلوب فصل استهلاك البيع عن السحب اليدوي وأي مرتجعات مشتريات مستقبلية.

### 3.4 سجل السحب الحالي

في `WithdrawnMaterialsTable.jsx`: المادة والدفعة والكمية وسعر الوحدة وقت السحب وإجمالي التكلفة والمورد والسبب والمنفّذ والتاريخ. نحتفظ بهذه المعلومات ونضيف توضيح الوحدتين والمرجع الثابت، ونحفظ مورد الدفعة التاريخي بدل الاعتماد على مورد المادة الحالي فقط.

المراجعة قراءة للكود وليست اختبار تشغيل للشاشات.

## 4. الوحدات والتحويل والكميات

### 4.1 تعريف الوحدة

لكل مادة وحدة كبيرة ووحدة صغيرة ومعامل تحويل ثابت ضمن تعريفها:

- كيلو جرام → جرام: 1000.
- لتر → ملليلتر: 1000.
- كرتونة → عبوة: عدد العبوات الذي يحدده الموظف للمادة، مثل 12 أو 24.

العلاقات الفيزيائية المعروفة ثابتة وغير قابلة للتعديل الحر. التغليف مثل الكرتونة متغير بين المواد. لا نحول وزنًا إلى حجم دون بيانات إضافية؛ كيلو إلى ملليلتر ليس زوجًا مقبولًا تلقائيًا.

المادة التي تُستهلك بالعبوات لا تُستهلك بالملليلتر في هذا التصميم دون تعريف إضافي لمحتوى العبوة. لم يحسم المستخدم هذه الحالة، فلا نفترض أن العبوة حجم ثابت.

### 4.2 إدخال الكمية

الكمية الصغيرة = الكمية الكبيرة × معامل التحويل.

حقل الكبيرة قابل للإدخال، والصغيرة للقراءة فقط. الخادم يعيد الحساب ولا يثق في قيمة محسوبة يرسلها العميل.

مثال: 2.5 كيلو → 2500 جرام، 3 لتر → 3000 ملليلتر، 2 كرتونة × 12 → 24 عبوة.

تُقبل كسور الكبيرة إذا أمكن تمثيل نتيجتها بدقة وفق خطوة الصغيرة. الوحدات المعدودة كالعبوة لا تقبل 2.4 عبوة ما لم نقرر صراحة السماح بجزء منها.

قيد يحتاج مراجعة استخدام حقيقية: ثلث كرتونة من 12 عبوة يساوي 4 عبوات، لكن إدخال الكبيرة بعدد عشري محدود قد ينتج 3.999996 عبوة. لا نقرب ذلك صامتًا. مقترح إضافة مُدخل كسري للكبيرة مثل `1/3` يُحوّل ككسر دقيق؛ إلى أن تُحسم واجهته، نرفض المدخل غير المطابق لخطوة الصغيرة برسالة واضحة. لا نغيّر رغبة المستخدم إلى إدخال الصغيرة دون اتفاق.

### 4.3 السعر والتكلفة

مقترح متسق مع النقاش: سعر الإدخال هو سعر وحدة كبيرة واحدة.

- تكلفة الصغيرة للعرض = سعر الكبيرة ÷ المعامل.
- إجمالي الشراء = كمية الكبيرة × سعر الكبيرة، مع سياسة خصومات وتكاليف إضافية مؤجلة للمشتريات.
- تكلفة البيع أو السحب تعتمد على تكلفة الدفعة التي خرجت منها الكمية، لا السعر المرجعي الحالي للمادة.
- تغير سعر شراء جديد لا يعدل أسعار الدفعات أو الحركات السابقة.

مثال: كيلو بسعر 600 جنيه → الجرام 0.6 جنيه → سحب 0.02 كيلو = 20 جرام بتكلفة 12 جنيه.

مثال تعدد دفعات: نحتاج 300 جرام، نأخذ 100 جرام من دفعة تكلفتها 0.6 للجرام و200 من دفعة تكلفتها 0.7؛ التكلفة الفعلية 60 + 140 = 200 جنيه.

## 5. دقة التخزين وتوزيع التكلفة

مقترح العملة EGP مثل وثيقة الموردين، والتأكيد النهائي عند الإعدادات. المبالغ والكميات تُنقل كسلاسل عشرية في API وتحسب بمكتبة Decimal، لا بـJavaScript Number.

- كميات الصغيرة ومعامل التحويل: `NUMERIC(20,6)`؛ حد أقصى 14 رقمًا قبل الفاصلة و6 بعدها. تراجع الحدود لكل عملية ضرب وجمع.
- أسعار الكبيرة والتكاليف الداخلية: `NUMERIC(24,8)`؛ 16 رقمًا قبل الفاصلة و8 بعدها، والتحقق من الحدود قبل الكتابة.
- سعر صغيرة قسمة قد تكون دورية؛ قيمته المعروضة إرشادية وليست وحدها أساس جمع التكاليف.
- كل دفعة تحتفظ بتكلفة أصلية دقيقة وقيمة متبقية. لتوزيع التكلفة: التكلفة التراكمية المستهدفة = تقريب إلى 8 منازل لـ(قيمة الدفعة الأصلية × مجموع الكمية الخارجة ÷ الكمية الأصلية). تكلفة الحركة الجديدة = الهدف الجديد − مجموع تكلفة الخروج السابقة.
- عند خروج آخر كمية، تُحمل عليها القيمة المتبقية بالضبط، فيصبح الرصيد الكمي والقيمي صفرًا دون بقايا تقريب.
- مجموع قيم الحركات مع المتبقي يساوي دائمًا قيمة الدفعة الأصلية. لا نقرب كل جزء إلى قرش قبل جمعه؛ العرض النهائي بالقرش لا يعيد كتابة السجل.
- المشتريات تحدد كيف توزع الخصومات والمصاريف والضرائب ضمن تكلفة الاستلام. إلى حينه التكلفة الأساسية هي الكمية × السعر، والوثيقة لا تفترض قواعد محاسبية أو ضريبية خارجية.

## 6. صفحة تعريف المادة والجدول

### 6.1 حقول التعريف المقترحة

- اسم المادة، إلزامي.
- المورد، إلزامي، اختيار من الموردين النشطين.
- الوحدة الكبيرة والصغيرة، إلزاميتان.
- معامل التحويل، ثابت تلقائيًا للأزواج المعروفة، ومدخل مطلوب للتغليف.
- السعر المرجعي للكبيرة، إلزامي في المقترح ويمكن أن يكون صفرًا مع وصفه بوضوح؛ لا ينشئ قيمة مخزون.
- الحد الأدنى للمخزون بالوحدة الكبيرة مع التحويل للصغيرة للعرض، غير سالب.
- تنبيه قرب الصلاحية بالأيام، اختياري؛ NULL يعني لا تنبيه مسبق. انتهاء الصلاحية لا يمنع البيع سواء كان التنبيه مفعّلًا أم لا.
- تاريخ ووقت الإنشاء والمنشئ تلقائيان، وآخر تعديل ومنفّذه محفوظان.

لا كمية افتتاحية مباشرة ولا تاريخ صلاحية عام للمادة؛ الصلاحية للدفعات وقد تختلف من توريد لآخر.

### 6.2 الجدول

أعمدة مقترحة: التسلسل، الاسم، المورد، الوحدتان، إجمالي الموجود بالصغيرة ومعادله بالكبيرة، المتاح للبيع، آخر سعر شراء للكبيرة، أقرب صلاحية لدفعة غير فارغة، حد التنبيه، والإجراءات.

قبل أول شراء يعرض «لم تُسجّل مشتريات» لآخر سعر، ويمكن عرض السعر المرجعي باسمه دون تسميته آخر سعر شراء.

التعديل يفتح محرر التعريف، والسحب ينقل إلى قسم السحب في تفاصيل المادة، والحذف يتبع قيوده. لا زر Refresh وفق تفسير الطلب، لكن القراءة تتحدث تلقائيًا بعد العمليات وعند إعادة فتح الصفحة؛ كيفية استقبال تغييرات مستخدم آخر تُحسم تقنيًا لاحقًا.

Pagination مقترحة 10 مواد للصفحة للاتساق مع الموردين، بحث بالاسم أو المورد، وترتيب `created_at DESC, id DESC`. فلترة البحث تعيد الصفحة 1. حجم الصفحة وحدود المواد لم يطلبهما المستخدم صراحة لهذا القسم ويظلان مقترحًا.

## 7. صفحة المادة والدفعات

### 7.1 بيانات المادة

تعرض الحقول العامة، والتواريخ، ومجموع المخزون وقيمته والمتاح للبيع، دون حساب الملخص من الدفعات الظاهرة فقط. المورد الحالي مرجع التعريف وليس بديلًا عن مورد الدفعة وقت الشراء.

### 7.2 بيانات كل دفعة

- رقم مرجعي واضح، ومرجع مستند وبند/استلام المشتريات.
- مورد التوريد التاريخي.
- الكمية الأصلية والمتبقية بالصغيرة وبالمكافئ الكبير.
- كمية البيع، وكمية التالف/الفاقد، وأي نوع خروج آخر مستقبلًا بشكل منفصل.
- سعر شراء الكبيرة، وتكلفة الصغيرة المحسوبة، والتكلفة الأصلية والمتبقية للدفعة.
- تاريخ الاستلام التجاري، وتاريخ الصلاحية الاختياري، ووقت التسجيل الفعلي ومنفّذه.
- ترتيب أولوية البيع، وحالة الكمية، وتنبيه الصلاحية المنفصل عنها.

Pagination مقترحة 10 دفعات، مع إمكانية رؤية الفارغة والتاريخية. إعادة ترتيب الأولويات تستخدم جميع الدفعات المؤهلة أو عملية نقل موضع محددة، وليس قائمة صفحة واحدة باعتبارها القائمة الكاملة.

الدفعة الفارغة لا تُحذف؛ تبقى دليلًا على كمية دخلت وكيف خرجت. لا يُسمح بتعديل سعر أو أصل كمية دفعة مرحّلة من هذه الصفحة.

## 8. إضافة الكميات من المشتريات فقط

Workflow مقترح:

1. تعريف المادة أولًا برصيد صفر.
2. من المشتريات: اختيارها ثم إدخال كمية الكبيرة؛ الصغيرة تظهر تلقائيًا.
3. إدخال سعر الكبيرة وتاريخ الاستلام والصلاحية إن وجدت.
4. اعتماد الاستلام هو نقطة إنشاء الدفعة وزيادة المخزون، وليس مجرد حفظ مسودة شراء.
5. كل بند استلام مستقل للمادة ينتج دفعة. استلام جزئي ثانٍ لنفس بند الشراء ينتج دفعة أخرى بمرجع استلام مختلف.
6. حفظ مصدر الاستلام والدفعة وحركة الإدخال وأثر المشتريات المالي معًا في المعاملة المناسبة.
7. تحديث قائمة المواد وصفحة المادة وصفحة المورد والتنبيهات.

نقطة اعتماد الشراء والتفريق بين الفاتورة والاستلام نهائيًا مؤجلان لتحليل المشتريات. العقد الثابت هنا: المسودة لا تزيد مخزونًا، ولا يوجد طريق عام لإضافة دفعة مباشرة من المواد.

ضمان التكرار: مصدر الاستلام له هوية فريدة، وإعادة نفس الاعتماد لا تنشئ دفعة ثانية. المبلغ المتساوي أو التاريخ المتساوي لا يعنيان وحدهما أن التوريد مكرر.

## 9. أولوية الخصم أثناء البيع

- الأولوية 1 تخصم أولًا، ثم التالية عند عدم كفاية الأولى.
- الافتراضي المقترح للدفعة الجديدة آخر الترتيب؛ يمكن تقديمها يدويًا. لا نفرض FIFO أو FEFO بدل اختيار المستخدم.
- يمكن التنبيه إلى دفعة أقرب للصلاحية دون تغيير ترتيب المستخدم تلقائيًا.
- تغيير الأولوية يؤثر على الخصم المقبل فقط، لا تخصيصات البيع المسجّلة سابقًا.
- الدفعة الفارغة مستبعدة من الخصم. الدفعة المنتهية ذات رصيد تدخل البيع بحسب أولويتها المعتادة؛ لا تتطلب تجاوزًا أو موافقة إضافية بسبب الصلاحية.
- تغيير ترتيب دفعة يعيد ترتيب الباقي دون تكرار مرتبة داخل المادة، وينفذ ذريًا مع نسخة ترتيب لمنع ضياع تعديل آخر.
- في الواجهة يمكن بقاء صفوف الدفعات بترتيب عرض ثابت، مع إظهار رقم الأولوية بوضوح؛ ترتيب العرض ليس ترتيب الخصم.

Workflow الخصم المقترح: الطلب يحدد احتياجات وصفة المنتج بالصغيرة → قفل المواد بترتيب معرّفات ثابت → اختيار الدفعات المؤهلة بحسب الأولوية → التحقق من كفاية كل المواد → تسجيل أجزاء الاستهلاك لكل دفعة وتكلفتها → اعتماد العملية كاملة.

إذا لم تكفِ مادة واحدة، لا تخصم باقي المواد وتترك طلبًا نصف منفذ. نقطة الخصم أثناء دورة الطلب (تأكيد/تحضير/إتمام) والحجز مؤجلة؛ تُطبق عند نقطة واحدة متفق عليها، لا عند كل انتقال حالة.

## 10. الموجود والمتاح والمنتهي والحجز

- **الموجود:** مجموع الكميات المتبقية في الدفعات، بما فيها المنتهية التي لم تُسحب فعليًا.
- **المتاح للبيع:** مجموع الكميات المتبقية، بما فيها المنتهية، مطروحًا منها الحجز إن اعتمد لاحقًا. تاريخ الصلاحية لا يخصم كمية من المتاح.
- **المسحوب:** كمية خرجت بحركة سحب نهائي ولا تدخل في الموجود أو المتاح.
- انتهاء الصلاحية تنبيه فقط: لا يمنع البيع ولا يغيّر الأولوية أو الكمية أو القيمة، ولا يُنشئ سحبًا تلقائيًا. الكمية تخرج بسحب نهائي صريح أو بحركة بيع فعلية.
- مقترح لحساب التنبيه فقط: تظهر منتهية في اليوم التالي لتاريخ الصلاحية بتوقيت القاهرة؛ هذا الوسم لا يؤثر في السماح بالبيع.
- تنبيه الحد الأدنى يستند إلى المتاح للبيع شاملًا رصيد الدفعات المنتهية. أقرب صلاحية للموجود تُظهر المنتهي بوسم واضح، مع استبعاد الفارغ والمسحوب بالكامل.

الحجز ليس متطلبًا نهائيًا الآن، فلا نضيف أرقامًا وهمية له. إذا اعتمد مع الطلبات، يجب أن تكون حجوزات المواد واضحة على دفعاتها أو بقاعدة تخصيص معلنة. السحب لا يستهلك كمية محجوزة؛ يلزم فك/إعادة تخصيص الحجز عبر مسار الطلب أولًا. لا يكفي طرح «المتاح» في الواجهة دون قفل وفحص في الخادم.

## 11. سحب التالف أو الفاقد

### 11.1 النموذج

المادة محددة من الصفحة، ثم اختيار دفعة بها كمية، وإظهار المتبقي، وإدخال الكمية بالكبيرة بجوار الصغيرة المحسوبة، وسبب إلزامي، ومعاينة تكلفة السحب. تاريخ ووقت السحب من الخادم عند التنفيذ.

سحب التالف والسحب النهائي اسمان لنفس العملية WITHDRAWAL، وليس مرحلتين أو خصمين منفصلين. يمكن سحب دفعة منتهية الصلاحية أو غير منتهية عند الحاجة؛ الانتهاء وحده لا ينفذ السحب ولا يمنع البيع. السحب هنا مختلف عن مرتجع للمورد أو استهلاك وصفة أو صرف نقدي.

### 11.2 التنفيذ

1. الموظف يراجع المادة والدفعة والكمية والسبب والتكلفة، ويؤكد الإجراء النهائي.
2. الخادم يتحقق من صلاحية الموظف لتنفيذ السحب ومفتاح منع التكرار والمقادير.
3. داخل Transaction يقفل المادة ثم الدفعة، ويعيد حساب الرصيد والتكلفة.
4. ينقص متبقي الدفعة ويكتب حركة WITHDRAWAL غير قابلة للتعديل ويسجل أثرها وتدقيقها.
5. يُعتمد الكل أو يُرجع الكل عند الفشل.
6. تعاد الكمية والقيمة الجديدتان ومرجع السحب، وتتحدث الواجهات والتنبيهات.

لا نتوقف عند التحقق في المتصفح: إذا باع موظف آخر جزءًا من الدفعة بعد فتح النموذج، يفحص الخادم الرصيد الجديد قبل السحب.

### 11.3 الأثر المالي

قيمة المخزون تنخفض بتكلفة الكمية من دفعتها، ويظهر نفس الأثر كخسارة تالف/فاقد في التقرير المناسب. هذه قاعدة لتتبع تكلفة النظام وليست فتوى ضريبية أو قيد حسابات عام نهائي.

لا نمحو تكلفة الشراء أو تاريخ الحركة، ولا نعتبر التالف مبيعات، ولا نقلل حساب المورد أو نحرك الدرج بسبب السحب وحده. مطالبات التعويض والمرتجعات للمورد دورة مختلفة.

ممنوع احتساب التالف مرتين: تكلفة البيع من حركات SALE_CONSUMPTION فقط، وخسائر السحب من WITHDRAWAL فقط، والتقرير يجمع كل فئة مرة واحدة.

### 11.4 النهائية

بناءً على طلب المستخدم لا يوجد restore أو تشغيل أو إعادة مخزون للكمية المسحوبة، ولا تعديل/حذف للسحب. الدفعة الأصلية تبقى ورصيدها ينقص؛ لا ننشئ نسخة مادة قابلة للتفعيل باسم «مسحوبة».

حتى السحب المسجّل بالخطأ لا توفر هذه النسخة زرًا لعكسه. لذا تلزم معاينة وتأكيد وصلاحية قبل التنفيذ. إذا احتاج النشاط لاحقًا معالجة فرق جرد، تُناقش كمتطلب مستقل ولا نضيف مسارًا يعيد الكمية ضمنيًا الآن. لا نسجّل مشتريات وهمية لتغطية خطأ.

## 12. قسم المواد المسحوبة

قائمة للقراءة والتصفية فقط، لكل حركة: مرجع، المادة، الدفعة، مورد الدفعة وقت التوريد، الكمية بالصغيرة والكبيرة، معامل التحويل وقت السحب، تكلفة الصغيرة للعرض، إجمالي التكلفة الفعلي، السبب، المنفّذ، وتاريخ ووقت التنفيذ.

Pagination مقترحة 10، وفلاتر المادة والمورد والدفعة والفترة والمنفّذ، وترتيب `recorded_at DESC, id DESC`. مجاميع التقرير تأتي من كل النتائج المطابقة، لا الصفحة الحالية.

يمكن جمع التكلفة في عملة واحدة، لكن لا يجمع التقرير جرامًا وملليلتر وعبوات في «إجمالي كمية» واحد. مجاميع الكميات تكون لكل مادة/وحدة على حدة.

لا إجراءات تعديل أو حذف أو استعادة. يمكن فتح مرجع الدفعة والمادة للقراءة. تحديث أسماء الموردين أو المادة لاحقًا لا يمحو اللقطة التاريخية المرتبطة بالسحب.

## 13. التواريخ وسجل الأحداث

- تعريف المادة: `created_at`, `created_by`, `updated_at`, `updated_by`.
- الدفعة: `received_on` كتاريخ تجاري، `expiry_on` اختياري، `recorded_at` الحقيقي ومنفّذه.
- البيع والسحب: `occurred_on` المشتق من وقت التنفيذ بالقاهرة، و`recorded_at` الحقيقي والمنفّذ.
- تغيير الأولوية: وقت ومنفّذ وترتيب قبل وبعد في التدقيق.
- الحذف المسموح لتعريف غير مستخدم: يحفظ التدقيق اسم المادة ومعرفها وسبب الحذف والوقت دون FK يمنع الاحتفاظ بالأثر بعد حذف التعريف.

تخزن اللحظات UTC وتعرض عبر `Africa/Cairo` دون إزاحة ثابتة. DATE لا يتحول إلى UTC. يرفض تاريخ استلام مستقبلًا، وتاريخ صلاحية قبل الاستلام، وأي تاريخ مستحيل. التوريد القديم الذي انتهت صلاحيته بالفعل يظهر بتنبيه ولا يُمنع استلامه أو بيعه بسبب الانتهاء وحده، ما دامت بقية بيانات الاستلام صحيحة.

السحب العادي وقته الآن ولا يُرجع للماضي لتعديل فترات قديمة. تواريخ الاستلام السابقة يمكن السماح بها بصلاحية وملاحظات مع الاحتفاظ بوقت الإدخال الحقيقي، وفق سياسة المشتريات والفترات المالية لاحقًا.

## 14. التعديل والحذف والتحقق

- يسمح بتعديل الاسم والتنبيهات والسعر المرجعي مع سجل تغيير؛ السعر المرجعي لا يعدل تكلفة المخزون.
- تثبت الوحدات والمعامل وخطوة الصغيرة بعد أول دفعة أو استخدام في وصفة منتج، حتى لو عاد الرصيد صفرًا؛ لحماية معنى التاريخ والوصفة. قبل الاستخدام يمكن التعديل مع إعادة حساب حد التنبيه وتسجيله.
- المورد الحالي يمكن تغييره في المقترح بصلاحية إلى مورد نشط؛ الدفعات القديمة تحتفظ بموردها التاريخي. توافق ذلك مع الشراء من عدة موردين يُراجع في المشتريات.
- الحذف النهائي لتعريف لم يدخل أي دفعة ولا حركة ولا وصفة ولا مرجع مشتريات/طلب، ورصيده صفر. الفحص تحت قفل وبـFK RESTRICT لمنع سباق شراء وحذف.
- مادة مستخدمة لا تُحذف حتى لو نفدت؛ نعرض السبب. إيقاف مواد مستخدمة ميزة غير مطلوبة هنا ولا تُضاف ضمنيًا.
- الكمية موجبة ومتوافقة مع دقة الوحدة؛ السحب لا يتجاوز المتبقي القابل للسحب.
- معامل التحويل موجب والأزواج صحيحة، والعدّ الصحيح مطلوب للعبوات.
- السعر غير سالب؛ سعر صفر يقبل فقط في استلام مجاني صريح تحدد سياسته المشتريات، ولا يمر بطريق إسقاط حقل السعر.
- النصوص الإلزامية غير فارغة بعد trim، وطولها محدود. تشابه أسماء المواد تنبيه وليس UNIQUE تلقائيًا.
- الأخطاء لا تظهر كرصد صفر أو نجاح جزئي. فشل تحميل السعر لا يتحول إلى صفر محفوظ.

## 15. التزامن ومنع التكرار

ترتيب القفل الداخلي: المواد حسب معرّف ثابت، ثم دفعاتها حسب معرّف ثابت. اختيار الأولوية يتم بعد قفل المادة؛ الأولوية لا تغير ترتيب القفل التقني. يجب توحيد ترتيب هذه الأقفال مع حساب المورد والدرج عند تصميم معاملة المشتريات متعددة المودولات.

التوريد والسحب والبيع وتغيير الأولوية والحذف تتبع نفس قفل المادة. لا تخصم عمليتان نفس الرصيد، ولا يعيد تغيير الأولوية توزيع حركة سبق اعتمادها.

يستخدم السحب `Idempotency-Key` وجدول `operation_requests` المشترك المقترح في وثيقة الموردين؛ نفس المفتاح والمحتوى يعيدان النتيجة نفسها، ونفس المفتاح بمحتوى مختلف يُرفض. لا حذف سريع لمفاتيح عمليات المخزون الناجحة يعرضها للتكرار.

مصدر استلام المشتريات فريد للدفعة، ومصدر استهلاك البيع له مفتاح تخصيص فريد لكل مادة ودفعة؛ تكرار أحداث الطلب لا يكرر الخصم. كل ذلك مع القيود وحركة التدقيق داخل Transaction واحدة، لا تحديثات HTTP منفصلة بلا ضمان ذري.

## 16. Data Flow ومصادر الحقيقة

تعريف مادة → ربط المورد والوحدات → شراء معتمد/استلام → دفعة وحركة دخول → ملخص المخزون.

وصفة منتج → احتياج بالصغيرة → أولوية دفعات → تخصيص بيع لكل دفعة وتكلفته → نقص المتبقي → تكلفة البيع في التقارير.

صفحة المادة → دفعة وكمية وسبب → حركة سحب نهائي → نقص المتبقي وقيمته → سجل المواد المسحوبة وخسائر التالف.

سجل الحركات مصدر تاريخ الكمية والتكلفة. الدفعة تحتفظ بإسقاط متزامن للمتبقي لتسهيل القفل. ملخص المادة يجمع إسقاطات كل الدفعات من الخادم؛ لا نخزن مجموعًا مستقلًا آخر دون حاجة.

سجل المواد المسحوبة هو عرض لحركات WITHDRAWAL، وليس نسخة قابلة للتعديل من المخزون. السعر الأخير للعرض معلومة مستقلة عن متوسط التكلفة أو تكلفة البيع الفعلية.

## 17. Schema — المبادئ والعلاقات

كل الحقول إلزامية إلا ما كُتب أنه اختياري. جميع FK تستخدم RESTRICT للحفاظ على التاريخ ما لم يذكر استثناء التدقيق للحذف المسموح. البيانات تأتي من الخادم أو مسار ترحيل موثوق، وليست كلها حقول إدخال.

الجداول الأساسية المقترحة:

1. `measurement_units`: قاموس وحدات صغير.
2. `raw_materials`: تعريف المادة والوحدات والتنبيهات.
3. `raw_material_batches`: مصدر كل توريد ولقطاته وأرصدته.
4. `inventory_movements`: سجل الدخول والبيع والسحب، ومنه سجل المواد المسحوبة.

جدول منع التكرار والتدقيق والموظفين والموردين مشتركة. تصميم المشتريات والطلبات ووصفات المنتجات مؤجل، ويذكر منه عقد الربط فقط.

العلاقات: مورد 1 ← مواد N؛ مادة 1 ← دفعات N؛ دفعة 1 ← حركات N؛ وحدة 1 ← تعريفات مواد N؛ بند استلام 1 ← دفعة 1 في هذا العقد؛ مصدر استهلاك بيع 1 ← تخصيصات دفعات N.

## 18. جدول measurement_units

- `id` — UUID، PK.
- `code` — VARCHAR(32)، UNIQUE ثابت مثل KG/G/L/ML/CARTON/PACK.
- `name_ar` — VARCHAR(60)، الاسم العربي غير فارغ.
- `kind` — VARCHAR(16)، MASS أو VOLUME أو COUNT أو CONTAINER.
- `physical_factor` — NUMERIC(20,6)، اختياري؛ للوزن بالنسبة للجرام وللحجم بالنسبة للملليلتر فقط.
- `created_at` — TIMESTAMPTZ من الخادم.

أزواج KG/G وL/ML تراجع بناءً على نوعهما ومعاملهما الثابت. CARTON/PACK علاقة تغليف يحدد معاملها في المادة. ليست هناك مقارنة وزن/حجم اعتمادًا على الاسم العربي. بيانات هذا القاموس تزرع بنسخة النظام ولا تُعدل الوحدات المستخدمة بصمت؛ إضافة وحدات لاحقًا قرار إعدادات.

CHECK للنوع، و`physical_factor > 0` عند وجوده؛ يُمنع استخدامه كتحويل بين أبعاد مختلفة. فهرس UNIQUE للكود كافٍ مبدئيًا.

## 19. جدول raw_materials

- `id` — UUID، PK.
- `name` — VARCHAR(150)، غير فارغ بعد التنظيف.
- `supplier_id` — UUID، FK إلى المورد الحالي، إلزامي.
- `large_unit_id` — UUID، FK للوحدة الكبيرة.
- `small_unit_id` — UUID، FK للصغيرة؛ مختلفة عن الكبيرة.
- `conversion_factor` — NUMERIC(20,6)، عدد الصغيرة في الكبيرة، CHECK > 0.
- `small_quantity_step` — NUMERIC(20,6)، أصغر خطوة مقبولة بالصغيرة، CHECK > 0؛ مقترح 1 للعبوة و0.001 للجرام/الملليلتر إذا احتاج النشاط ذلك.
- `reference_large_unit_price` — NUMERIC(24,8)، CHECK >= 0؛ سعر مرجعي فقط.
- `currency` — CHAR(3)، EGP افتراضيًا ومقيد بها في النسخة أحادية العملة.
- `min_stock_small` — NUMERIC(20,6)، CHECK >= 0؛ الحد بعد التحويل من إدخال الكبيرة.
- `expiry_alert_days` — INTEGER، اختياري، CHECK >= 0 عند وجوده.
- `created_at`, `updated_at` — TIMESTAMPTZ، من الخادم.
- `created_by`, `updated_by` — UUID، FK موظفين؛ الأخير يساوي المنشئ أولًا.
- `version` — BIGINT، افتراضي 1، موجب؛ لتعديل التعريف.
- `priority_version` — BIGINT، افتراضي 0، غير سالب؛ يتغير مع إضافة دفعة أو تغيير ترتيبها.

قيود خدمة/قاعدة ترحيل: توافق الوحدات والمعامل، وتوافق الحد مع الخطوة، ومنع تغيير الوحدات بعد الاستخدام. فحص المورد النشط عند إنشاء/تغيير العلاقة. لا CHECK بسيط يكفي للتحقق من صفوف الجداول المرتبطة.

الفهارس: `(supplier_id)`، و`(created_at DESC,id DESC)` للقائمة. بحث جزئي قد يحتاج trigram إذا كبرت البيانات، ولا نفترض أن B-tree يسرع كل contains.

لا `quantity` يُعدل يدويًا في هذا الجدول، ولا صلاحية موحدة، ولا تكلفة فعلية ثابتة لكل دفعات المادة.

## 20. جدول raw_material_batches

### 20.1 الهوية والمصدر

- `id` — UUID، PK.
- `material_id` — UUID، FK إلى المادة.
- `batch_number` — BIGINT موجب، UNIQUE داخل المادة، مرجع عرض ثابت.
- `purchase_receipt_item_id` — UUID، FK UNIQUE إلى بند استلام مشتريات مرحّل؛ اسم الجدول النهائي مؤجل.
- `supplier_id` — UUID، FK المورد الفعلي وقت التوريد.
- `supplier_name_snapshot` — VARCHAR(150)، لقطة اسم المورد وقت الاستلام.
- `material_name_snapshot` — VARCHAR(150)، لقطة اسم المادة وقت الاستلام.

### 20.2 الكميات والوحدات التاريخية

- `large_unit_id`, `small_unit_id` — UUID، FK للوحدات التي جرى الاستلام بها.
- `conversion_factor_snapshot` — NUMERIC(20,6)، موجب، لقطة معامل التحويل.
- `small_quantity_step_snapshot` — NUMERIC(20,6)، موجب.
- `initial_quantity_small` — NUMERIC(20,6)، موجب، أصل كمية الاستلام.
- `remaining_quantity_small` — NUMERIC(20,6)، بين صفر والأصل.

الكمية الكبيرة الأصلية مشتقة من الأصل الصغير والمعامل، وليس لها مخزن عشري ثانٍ قد يتناقض معه. القيمة المدخلة نصًا/كسرًا يمكن الاحتفاظ بها في بند الاستلام لمنع فقد معنى الإدخال دون استخدامها كرصيد مستقل.

### 20.3 التسعير والقيمة

- `purchase_large_unit_price` — NUMERIC(24,8)، غير سالب، سعر الكبيرة وقت الاستلام.
- `initial_inventory_value` — NUMERIC(24,8)، غير سالب، التكلفة المخصصة للدفعة من المشتريات بعد قواعدها عند اعتمادها.
- `remaining_inventory_value` — NUMERIC(24,8)، بين صفر والقيمة الأصلية.
- `currency` — CHAR(3)، متطابقة مع المادة/مستند الشراء.

تكلفة الصغيرة = القيمة الأصلية ÷ الكمية الأصلية للعرض والتخصيص؛ قد تختلف عن سعر الكبيرة ÷ المعامل إذا أضيفت تكاليف شراء مستقبلًا. يجب تسمية «سعر الشراء» و«تكلفة المخزون» بشكل واضح.

### 20.4 التواريخ والأولوية

- `received_on` — DATE، تاريخ الاستلام التجاري.
- `expiry_on` — DATE اختياري، لا يقل عن الاستلام.
- `recorded_at` — TIMESTAMPTZ، وقت إدخال الدفعة الحقيقي.
- `recorded_by` — UUID، FK موظف.
- `sale_priority` — INTEGER موجب، UNIQUE داخل المادة، قابل للتغيير بخدمة الترتيب.
- `priority_changed_at` — TIMESTAMPTZ، اختياري حتى أول تغيير يدوي.
- `priority_changed_by` — UUID، اختياري مع السابق.
- `version` — BIGINT موجب افتراضي 1، يزيد مع حركة مخزون أو تغيير ترتيب.
- `balance_updated_at` — TIMESTAMPTZ، وقت آخر ترحيل، يبدأ بوقت التسجيل.

الفهارس والقيود:

- UNIQUE `(material_id,batch_number)` وUNIQUE `(material_id,sale_priority)` مؤجل للتحقق عند نهاية إعادة الترتيب.
- UNIQUE على مصدر الاستلام يمنع إعادة اعتماد نفس البند.
- `(material_id,sale_priority)` يخدم اختيار البيع، و`(material_id,received_on DESC,id DESC)` للقائمة.
- فهرس الصلاحية للدفعات ذات المتبقي موجب عند الحاجة للتنبيهات.
- الرصيد الكمي صفر يستلزم القيمة المتبقية صفر؛ الرصيد الموجب قد قيمته صفر لتوريد مجاني معتمد.
- الأصل والوحدات والسعر والتواريخ ومصدر الاستلام غير قابلة للتعديل من صفحة المادة بعد الترحيل.
- أرقام الكميات المستهلكة والمسحوبة تجمع من الحركات، ولا نضيف حقولًا مستقلة قابلة للانحراف عن السجل.
- حالة الكمية (فارغة/بها رصيد) مستقلة عن تنبيه الصلاحية (منتهية/قريبة/غير ذلك). كلاهما مشتق؛ انتهاء الصلاحية لا يجعل الرصيد غير متاح ولا يفرض قيد منع بيع في الـSchema.

## 21. جدول inventory_movements

### 21.1 الحقول

- `id` — UUID، PK.
- `batch_id` — UUID، FK للدفعة؛ المادة والمورد والوحدات التاريخية متاحة عبر الدفعة ولا تُكرر كـFK دون داعٍ.
- `sequence_no` — BIGINT موجب، ترتيب الحركة داخل الدفعة تحت القفل.
- `kind` — VARCHAR(32): PURCHASE_RECEIPT أو SALE_CONSUMPTION أو WITHDRAWAL في النطاق الحالي.
- `quantity_small` — NUMERIC(20,6)، موجب دائمًا؛ اتجاه الكمية مشتق من النوع.
- `inventory_value` — NUMERIC(24,8)، غير سالب، قيمة حركة الدخول أو تكلفة الخروج الفعلية.
- `quantity_after_small` — NUMERIC(20,6)، غير سالب، لقطة بعد الحركة.
- `inventory_value_after` — NUMERIC(24,8)، غير سالب، لقطة القيمة بعد الحركة.
- `occurred_on` — DATE، التاريخ التجاري للحركة.
- `recorded_at` — TIMESTAMPTZ، وقت التسجيل الحقيقي من الخادم.
- `recorded_by` — UUID، FK الموظف/الفاعل المعتمد، لا يختاره العميل بحرية.
- `reason` — VARCHAR(1000)، اختياري عمومًا، إلزامي غير فارغ في WITHDRAWAL.
- `material_name_at_movement` — VARCHAR(150)، لقطة الاسم وقت الحركة للعرض التاريخي.
- `operation_request_id` — UUID، FK إلى جدول منع التكرار المشترك.
- `source_sale_consumption_id` — UUID، FK اختياري إلى مصدر استهلاك بيع، مطلوب فقط في SALE_CONSUMPTION؛ الجدول النهائي مؤجل.

لا `updated_at` ولا `restored_at` ولا `is_active` للحركة؛ هي حدث ثابت لا يُشغّل ويُوقف. لعرض سجل السحب نرشح WITHDRAWAL ونربط الدفعة ووحداتها ولقطة المورد.

### 21.2 القيود والفهارس

- UNIQUE `(batch_id,sequence_no)`.
- فهرس UNIQUE جزئي على `batch_id` لنوع PURCHASE_RECEIPT؛ دخول أصلي واحد لكل دفعة.
- UNIQUE جزئي `(source_sale_consumption_id,batch_id)` لاستهلاك البيع؛ المصدر يمثل مادة محددة في تخصيص البيع، وتجمع حصتها لكل دفعة في حركة واحدة.
- UNIQUE جزئي على `operation_request_id` لنوع WITHDRAWAL: السحب الحالي يخص دفعة واحدة لكل طلب. بيع واحد قد ينتج حركات متعددة، لذلك لا نجعل هذا المرجع UNIQUE على الجدول كله.
- CHECK موجب للكمية، وغير سالب للقيم، وصحة النوع، وتوافق وجود مصدر البيع والسبب مع النوع.
- `(recorded_at DESC,id DESC) WHERE kind='WITHDRAWAL'` لسجل السحب.
- `(batch_id,sequence_no)` يخدم مراجعة الحركات، وفهارس الفلاتر الأخرى حسب القياس.
- منع UPDATE/DELETE بامتيازات قاعدة البيانات أو Trigger.
- إجراء الترحيل يضمن توافق كل حركة مع رصيد الدفعة وخطوة الوحدة وقيمتها، وكتابة الإسقاط واللقطة والسجل معًا.
- تحقق مؤجل أو إجراء موحد يضمن وجود حركة دخول واحدة لكل دفعة جديدة، مساوية لكميتها وقيمتها الأصليتين؛ الفهرس وحده يمنع الأكثر من واحدة ولا يضمن الوجود.

## 22. معادلات الاتساق

في النطاق الحالي:

متبقي الدفعة = كمية PURCHASE_RECEIPT − مجموع SALE_CONSUMPTION − مجموع WITHDRAWAL.

قيمة الدفعة المتبقية = قيمة PURCHASE_RECEIPT − تكلفة SALE_CONSUMPTION − تكلفة WITHDRAWAL.

إجمالي المادة الموجود = مجموع متبقي جميع دفعاتها. المسحوب لا يدخل هذا المجموع مرة أخرى. تكلفة الخسائر = مجموع قيمة WITHDRAWAL فقط.

يجب تطابق مجموع الحركات مع إسقاط الدفعة ومع لقطة آخر حركة. لا نصلح خللًا بكتابة رصيد جديد أو `max(0, balance)`؛ التحقيق والتصحيح لهما مسار معلن.

مرتجعات المشتريات وإلغاء البيع ليست ضمن الأنواع الثلاثة حاليًا. ستحتاج امتدادًا صريحًا للسجل ومعادلاته بعد تحليلها؛ لا يعاد استخدامها كـWITHDRAWAL لإخفاء اختلاف معناها، ولا يسمح أي امتداد باستعادة سحب التالف خلاف طلب المستخدم.

## 23. عقود الربط مع باقي المودولات

### الموردون

مورد المادة الحالي يحدد ظهورها في صفحة المورد واختيارات الشراء المبدئية. مورد الدفعة لقطة تاريخية مستقلة عن أي تغيير لاحق. إنشاء المادة وربطها لا ينشئ دينًا أو مستحقًا ولا نقدًا.

### المشتريات

هي المصدر الوحيد لإنشاء الدفعات. نحتاج هوية بند استلام فريدة ومعرّف مادة ومورد وتواريخ ووحدات وكمية وتكلفة وفاعل. إدخال أثر المورد والدرج يتم عبر خدماتهما دون قيد مالي يدوي إضافي من المخزون.

### المنتجات والطلبات

الوصفة ستحدد احتياجًا بوحدة المادة الصغيرة. الطلب يطلب تخصيصًا للدفعات بحسب الأولوية ويحفظ تكلفة كل تخصيص. لحظة الخصم والحجز والإلغاء والمرتجع ما زالت مؤجلة؛ لا يوجد API عام متاح للموظف لتسمية أي خصم «بيعًا» دون مصدر طلب صالح.

### التقارير

تميّز بين قيمة المخزون الحالي، تكلفة المبيعات، تكلفة التالف، والمشتريات النقدية/الآجلة. لا تجمع الكمية بين وحدات مختلفة، ولا تعتمد على سعر المادة المرجعي لتقييم جميع دفعاتها.

### سجل الأحداث

تعريف وتعديل وحذف مسموح، وتغيير مورد أو أولوية، واستلام، واستهلاك، وسحب: كل حدث له هوية كيان ووقت ومنفّذ ومرجع طلب وسبب عند الحاجة. السجل المالي/المخزني ليس بديلًا عن فروق تعديل الحقول العامة.

## 24. الصلاحيات المقترحة

- `materials.read`: عرض المواد والدفعات.
- `materials.create`: تعريف مادة.
- `materials.update`: تعديل التعريف ضمن القيود.
- `materials.delete_unused`: حذف تعريف غير مستخدم فقط.
- `materials.reorder_batches`: تغيير أولوية البيع.
- `materials.withdraw`: تنفيذ السحب النهائي.
- `materials.withdrawals.read`: عرض سجل المسحوب.
- `materials.cost.read`: الاطلاع على الأسعار والتكلفة، حسب توزيع الأدوار لاحقًا.

إضافة دفعات تحتاج صلاحية اعتماد الاستلام من المشتريات ومسارها، ولا تمنح ضمن تعديل المادة. التنفيذ الفعلي للبيع يحتاج صلاحيات ومسار الطلب المناسبين. كل فحص في الخادم، وليس فقط إخفاء أزرار.

## 25. API مقترح

المسارات للتصميم فقط، ولا تعني وجود باك منفّذ:

- `GET /api/measurement-units`: الأزواج/الوحدات المتاحة وقواعد التحويل.
- `POST /api/raw-materials`: تعريف دون كمية أو دفعة.
- `GET /api/raw-materials?page=1&search=...`: بيانات وملخصات صحيحة من كل الدفعات.
- `GET /api/raw-materials/:id`: تعريف وملخص وتواريخ.
- `PATCH /api/raw-materials/:id`: حقول عامة مسموحة مع `expectedVersion`.
- `DELETE /api/raw-materials/:id`: فقط إن لم تستخدم، مع سبب وتدقيق.
- `GET /api/raw-materials/:id/batches?page=1`: دفعات مقسمة بملخصات مستقلة.
- `PATCH /api/raw-materials/:id/batch-priority`: معرّف الدفعة ومعرّف الدفعة التي توضع قبلها أو موضع نهاية، و`expectedPriorityVersion`؛ الخادم يعيد ترتيب المجموعة كاملة.
- `POST /api/raw-materials/:id/withdrawals`: دفعة، كمية كبيرة، سبب، ومفتاح منع التكرار.
- `GET /api/material-withdrawals?page=1&materialId=...&from=...&to=...`: السجل والفلاتر.

لا POST عام لإضافة دفعة من شاشة المواد، ولا PATCH لكميات الدفعات، ولا DELETE للدفعات المرحلة، ولا restore للسحب.

مثال جسم سحب:

```json
{
  "batchId": "UUID-of-selected-batch",
  "quantityLarge": "0.020000",
  "reason": "تلف الكمية بسبب سوء التخزين"
}
```

معامل التحويل والتكلفة والفاعل والتاريخ تؤخذ من بيانات موثوقة بالخادم. الاستجابة: مرجع السحب، المادة والدفعة، الكمية بالصغيرة ومكافئها الكبير، القيمة الفعلية، المتبقي الكمي والقيمي، وقت ومنفّذ العملية. الأخطاء لا تعيد نتيجة نصف ناجحة.

أكواد أخطاء مقترحة: `VALIDATION_ERROR`, `MATERIAL_NOT_FOUND`, `BATCH_NOT_FOUND`, `BATCH_MATERIAL_MISMATCH`, `INVALID_UNIT_PAIR`, `INVALID_QUANTITY_STEP`, `INSUFFICIENT_STOCK`, `STOCK_RESERVED`, `MATERIAL_IN_USE`, `UNITS_LOCKED`, `PRIORITY_VERSION_CONFLICT`, `PURCHASE_SOURCE_REQUIRED`, `IDEMPOTENCY_CONFLICT`, `FORBIDDEN`.

الأخطاء 400/422 للتحقق، 401/403 للدخول والصلاحية، 404 للكيان، 409 لتعارض رصيد/نسخة/استخدام. لا نكشف تفاصيل SQL. استجابة إعادة الطلب الناجح تعيد مرجعه الأصلي؛ الواجهة تجلب الملخص الأحدث إذا حدثت تغييرات لاحقة.

## 26. سيناريوهات القبول والتحقق عند التنفيذ

هذه معايير موثقة وليست اختبارات منفّذة في هذه المرحلة.

1. تعريف صالح ينتج مادة برصيد صفر وبيانات وتواريخ ومنشئ وعلاقة مورد.
2. لا ينشئ تعريف المادة دفعة أو دينًا أو حركة درج.
3. إدخال 2 كيلو يعرض 2000 جرام تلقائيًا؛ تغيير الكبيرة يغير الصغيرة.
4. الكرتونة ذات 12 عبوة والكمية 2 تنتج 24 عبوة.
5. تغيير الصغيرة أو التكلفة عبر طلب مزور لا يتجاوز حساب الخادم.
6. كيلو إلى ملليلتر ومعامل صفر أو سالب أو تجاوز الدقة يُرفض.
7. كمية ينتج عنها جزء غير مسموح من عبوة تُرفض دون تقريب صامت.
8. مسودة شراء لا تغير المخزون، واعتماد الاستلام ينشئ دفعة وحركة دخول مرة واحدة.
9. تكرار اعتماد مصدر الاستلام لا يكرر الدفعة، واستلام جديد مستقل ينشئ دفعة جديدة.
10. شراء المادة بسعر جديد يحافظ على سعر وقيمة وتواريخ الدفعات القديمة.
11. المورد التاريخي للدفعة يظل ظاهرًا بعد تغيير مورد التعريف.
12. كمية مطلوبة موزعة بين دفعتين تخصم بحسب الأولوية وتكلفة كل جزء بسعر دفعته.
13. تغيير الأولوية يعيد ترتيب الباقي دون تعادل، ويؤثر في البيع القادم فقط.
14. ترتيب دفعة من صفحة جزئية لا يمحو أولويات الدفعات غير الظاهرة.
15. دفعة منتهية ذات أولوية 1 وبها رصيد تدخل البيع أولًا دون منع أو تأكيد إضافي؛ يظهر تنبيه الصلاحية فقط. ويمكن سحبها نهائيًا صراحة.
16. عدم كفاية مادة واحدة لطلب يمنع الخصم الجزئي لبقية مواده.
17. بيع وسحب متزامنان لا ينتجان رصيدًا سالبًا أو استخدام نفس الكمية مرتين.
18. سحب صحيح ينقص الدفعة والمادة والقيمة ويظهر في السجل مع السبب والتاريخ والمنفّذ.
19. سحب كامل يجعل كمية وقيمة الدفعة صفرًا ويحافظ على تاريخها.
20. لا يوجد تعديل/حذف/استعادة لسحب مرحّل، حتى بإرسال طلب API مباشر غير موجود أو غير مسموح.
21. سحب صفر/سالب/أكبر من المتبقي أو من دفعة مادة أخرى يُرفض.
22. فشل بين كتابة الحركة وتحديث الإسقاط يرجع العملية كلها.
23. الضغط مرتين أو فقد الرد ثم إعادة نفس المفتاح ينتج سحبًا واحدًا.
24. نفس المفتاح بمحتوى مختلف يُرفض.
25. التالف يظهر كخسارة منفصلة، ولا يغير رصيد المورد أو الدرج ولا يتكرر ضمن تكلفة البيع.
26. أكثر من صفحة دفعات لا يغير صحة ملخص المخزون وقيمته.
27. اختلاف الوحدات لا ينتج مجموع كمية مضللًا في سجل السحب العام.
28. تغيير السعر المرجعي لا يعدل قيمة المخزون أو تكلفة مبيعات قديمة.
29. منع تعديل الوحدات بعد الاستخدام يحمي الكميات والوصفات والتاريخ، ولو المتبقي صفر.
30. حذف تعريف غير مستخدم ينجح مع التدقيق؛ المستخدم في دفعة أو وصفة أو مستند يُرفض.
31. مورد غير نشط لا يربط بمادة جديدة، والدفعات التاريخية لا تختفي بإيقافه.
32. حدود منتصف الليل بتوقيت القاهرة لا تغير اليوم التجاري خطأ، والصلاحية تتبع السياسة المعلنة.
33. تكرار خروج أجزاء صغيرة جدًا من دفعة ذات تكلفة دورية ينتهي دون بقايا كمية أو تكلفة، وتطابق القيم الأصلية.
34. فشل تحميل البيانات لا يظهر كرصيد أو سعر صفر قابل للحفظ.
35. صاحب صلاحية القراءة دون السحب لا ينفذه عبر API.
36. مراجعة جمع الحركات تطابق متبقي كل دفعة ولقطات آخر حركة.
37. عند اعتماد الحجز لاحقًا: السحب لا يأخذ كمية محجوزة دون معالجة الحجز من مساره.
38. صفحة المادة لا توفر إضافة كمية جانبية تتجاوز المشتريات.
39. بلوغ تاريخ الانتهاء لا يغيّر المتاح أو القيمة أو ترتيب الخصم ولا ينشئ حركة مخزون تلقائية.
40. سحب التالف ينشئ حركة WITHDRAWAL واحدة فقط؛ لا تنشأ حركة ثانية باسم السحب النهائي ولا تتكرر التكلفة.

## 27. نقاط مؤجلة ومقترحات تحتاج تثبيتًا

- التفسير النهائي لعبارة عدم وجود تحديث، وحجم صفحات المواد والدفعات والسحب المقترح 10.
- السعر المرجعي في تعريف المادة وتسميته مقابل سعر الدفعة الفعلي.
- التعامل مع كميات قائمة عند بدء النظام من قسم المشتريات دون التزامات مالية مختلقة.
- اختيار واجهة إدخال كسور الكبيرة غير المنتهية مثل ثلث الكرتونة، وقواعد أجزاء العبوة.
- وحدات خاصة مثل عبوة تُستهلك بالملليلتر وتحتاج مستوى تحويل إضافيًا؛ لا نفترضها تلقائيًا.
- نقطة اعتماد الاستلام والتوريد المجاني والتكاليف الإضافية والخصومات والضرائب وتصحيح فاتورة شراء.
- حجز مواد الطلب ولحظة الخصم وإلغاء البيع والمرتجعات، مع الحفاظ على نهائية سحب التالف.
- سياسة المورد الحالي إذا أصبح للمادة عدة مصادر شراء.
- إغلاق الفترات والتاريخ السابق وصلاحيات الأدوار؛ تاريخ الانتهاء وحده لا يغيّر قيمة المخزون في النطاق الحالي.
- مقادير الدقة والحدود والتقنية مقترحات هندسية تُراجع قبل التنفيذ.

## 28. مصادر مراجعة الفرونت

- `new desktop/src/modules/admin/inventory/components/AddMaterialForm.jsx`
- `new desktop/src/modules/admin/inventory/components/MaterialsTable.jsx`
- `new desktop/src/modules/admin/inventory/pages/MaterialDetailsPage.jsx`
- `new desktop/src/modules/admin/inventory/components/WithdrawnMaterialsTable.jsx`
- `new desktop/src/modules/admin/inventory/constants/inventoryConstants.js`
- `new desktop/src/modules/admin/inventory/services/inventoryService.js`

لم تُعدَّل ملفات الفرونت أو الباك الحالي عند إعداد هذه الوثيقة. أي اختلاف بينها وبين الواجهة القديمة مقصود لتلبية وصف النظام الجديد، مع تمييز ما يحتاج مراجعة بدل نقله تلقائيًا.


