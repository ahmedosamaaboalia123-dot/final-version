# Implementation Status — 404 Coffee Backend v2

آخر تحديث: 2026-09-12 — Africa/Cairo.

## الحالة العامة

- المشروع: Backend جديد مستقل من الصفر.
- التقنية: Node.js + Express + MongoDB + Mongoose، JavaScript ESM.
- المسار: `C:\Users\hp\Desktop\last 404\404-coffee-backend-v2`.
- مرجع التنفيذ: `backend-design/final-2`.
- الدفع: CASH فقط، بنمطي DIRECT وCOD.
- المرحلة الحالية: الدفعة 28 مكتملة ومتحقق منها على Atlas حقيقي.
- المرحلة التالية: التشغيل التجريبي الكامل وتبني شاشات الفرونت لعقد v1 تدريجيًا.

## حالات التنفيذ

- `NOT_STARTED`: لم يبدأ العمل.
- `IN_PROGRESS`: جارٍ التنفيذ.
- `IMPLEMENTED`: الكود موجود ولم يكمل بوابة التحقق.
- `VERIFIED`: التنفيذ والاختبارات والمعايير ناجحة.
- `BLOCKED`: عائق خارجي موثق يمنع التقدم.

## سجل الدفعات

### الدفعة 0 — فحص البيئة ومساحة العمل: VERIFIED

تم تنفيذ الآتي:

- فحص Node.js: `v20.18.0`.
- فحص npm: `10.8.2`.
- فحص Git: `2.53.0.windows.2`.
- فحص MongoDB المحلي: `mongod` غير مثبت أو غير موجود في PATH.
- فحص Docker: غير مثبت أو غير موجود في PATH.
- فحص قواعد مساحة العمل: لا يوجد `AGENTS.md` في الجذر؛ يوجد `new desktop/AGENTS.md` خاص بالفرونت، ويلزم Mobile-first وRTL ولا ينطبق على ملفات الباك المستقلة إلا عند تعديل الفرونت لاحقًا.
- فحص Git baseline: توجد تغييرات وحذف واسعة سابقة في الباك القديم وتغيير في `new desktop`. هذه التغييرات ليست جزءًا من v2 ولن يتم تعديلها أو استعادتها أو نسبها لهذا التنفيذ.
- مراجعة Frontend API surface الحالية من `src/services/endpoints.js`, `apiClient.js`, `realtime.js` ووحدات checkout/table/admin.
- إنشاء مجلد الباك الجديد وملفات التحكم فقط؛ لم يتم نسخ الباك القديم ولم يتم تثبيت Dependencies.

### بوابة تحقق الدفعة 0

- [x] المسار الجديد مستقل.
- [x] لم يتم نسخ كود من الباك القديم.
- [x] لم يتم تعديل ملفات `backend-design/final`.
- [x] تم تسجيل إصدارات الأدوات.
- [x] تم تسجيل حالة Mongo/Docker بوضوح.
- [x] تم تسجيل Git baseline السابق.
- [x] تم حصر واجهة API الحالية للفرونت والفجوة مع v2.
- [x] ملفات التحكم الأربعة موجودة وصالحة.

## توافق الفرونت المسجل

الفرونت الحالي يستخدم `VITE_API_BASE_URL=http://localhost:5000/api` ومسارات أقدم، منها `/users`, `/orders/public`, `/products/public`, `/table-sessions`. العقد الجديد يستخدم `/api/v1` ومسارات screen/public جديدة. لن ننشئ Compatibility aliases عشوائيًا داخل الباك؛ في مرحلة الربط سيُحدّث `endpoints.js` وAPI base بصورة مركزية، أو يضاف Adapter موثق إذا احتجنا انتقالًا تدريجيًا.

## الدفعة 1 — Runnable scaffold: VERIFIED

تم إنشاء مشروع Node.js ESM مستقل، وتثبيت وقفل حزم التشغيل والتطوير، وإضافة تحميل إعدادات typed وآمنة، وإعدادات العمل الثابتة، واتصال Mongoose، وExpress app factory، وbootstrap/shutdown، ومغلفات الاستجابة والأخطاء، وrequest ID، ومسارات الصحة والإصدار.

### بوابة تحقق الدفعة 1

- [x] `npm run lint` — ناجح بلا أخطاء.
- [x] `npm test` — ملفا اختبار و7 اختبارات ناجحة.
- [x] `npm run format:check` — كل الملفات مطابقة لـPrettier.
- [x] `GET /health/live` لا يعتمد على MongoDB.
- [x] `GET /health/ready` يعيد 200 عند الجاهزية و503 retryable عند تعطل dependency مطلوبة.
- [x] `GET /system/version` يعرض إصدار العقد والبناء.
- [x] bootstrap يمكن إنشاؤه للاختبار من غير اتصال حقيقي، وshutdown يغلق الموارد ويلغي مؤقت المهلة.
- [x] Secrets محجوبة من safe config.

### الإصدارات المثبتة

- Runtime: Express 5.2.1، Mongoose 8.24.4، Zod 4.6.2، dotenv 17.4.2، Helmet 8.3.0، CORS 2.8.6، Compression 1.8.2.
- Tooling: Vitest 4.1.11، Supertest 7.2.2، ESLint 9.39.5، Prettier 3.9.6.

## الدفعة 2 — Platform Core: VERIFIED

تم إنشاء الأساس الموحد الذي ستستخدمه كل الموديولات اللاحقة:

- `ApiError` وassertion وasync handler وmapping آمن لأخطاء Mongo وحجم الطلب.
- Success/Created/List/Accepted/Service Unavailable envelopes موحدة تحمل requestId وserverTime.
- request/correlation context باستخدام `AsyncLocalStorage`، وstructured logger مع redaction متداخل.
- Zod validation middleware صارم يرفض الحقول غير المعروفة عند استخدام schemas strict.
- Pagination بحد أقصى 10 وصف ثابت مع `_id` كفاصل نهائي.
- حساب Decimal دقيق بلا Float وسيط، مع سياسات مستقلة للنقود والكميات وتحويل Decimal128.
- Transaction runner يعيد استخدام الجلسة الأصلية، ويعيد transient transaction مرتين فقط، ويدعم حسم Unknown Commit.
- Sequence allocator ذري لاستخدام event numbers لاحقًا.
- OperationRequest model وخدمة Idempotency مع canonical request hash، وإعادة النتيجة ومنع إعادة استخدام المفتاح مع Body مختلف.
- AuditEvent model وwriter مع allowlisted changes وredaction وإسناد eventNo تلقائيًا.
- Transactional Outbox model/writer وEvent Bus وlease/claim/publish/retry/dead-letter operations.
- Auth context interfaces ثابتة للزائر والموظف، مع permission checks.

### بوابة تحقق الدفعة 2

- [x] `npm run lint` — ناجح بلا أخطاء.
- [x] `npm test` — 5 ملفات اختبار و22 اختبارًا ناجحًا.
- [x] `npm run format:check` — كل الملفات مطابقة لـPrettier.
- [x] Decimal boundaries والجمع والضرب والقسمة وتقريب النقود والكميات.
- [x] Pagination لا تتجاوز 10 وترتيبها deterministic.
- [x] رفض operator/unknown field عبر strict validation.
- [x] Double idempotency يعيد العملية نفسها، والمفتاح نفسه مع Body مختلف يعيد 409.
- [x] Transaction wrapper يمنع nested transaction، يعيد transient failure، ويختبر rollback متعدد المخازن بمحاكاة session.
- [x] Outbox يظل محفوظًا إذا فشل publisher بعد الحفظ.
- [x] Error responses لا تعرض stack أو cause أو secret، و413 يحمل requestId.
- [x] Audit وOutbox يزيلان password/token من البيانات المحفوظة.

التحقق على MongoDB Replica Set حقيقي ما زال ضمن بوابة Integration اللاحقة لأن runtime غير متاح على الجهاز، ومثبت بوضوح في `KNOWN_LIMITATIONS.md`.

## الدفعة 3 — الموظفون والمصادقة والصلاحيات: VERIFIED

تم تنفيذ الموديول المستقل وربطه تحت `/api/v1`:

- Models كاملة للموظف والدور وكتالوج الصلاحيات وربط الدور والصلاحيات والاستثناءات الفردية وإظهار الصفحات.
- كلمة المرور محفوظة plaintext حسب قرار المنتج، مع `select:false`، ولا تظهر في DTO إلا عند طلبها ووجود `employees.password.view`.
- EmployeeDevice يحتفظ ببصمة الجهاز كحقل محجوب وبـhash للفهرسة، ويدعم PENDING/APPROVED/BLOCKED وأكثر من جهاز للموظف.
- AuthSession يخزن refresh token hash فقط، مع TTL، rotation، revocation، وربط الموظف والجهاز ونسخة الصلاحيات.
- Access token موقع بـHMAC ويحمل employee/session/device/permissionsVersion ومدة 15 دقيقة افتراضيًا.
- LoginAttempt مستقل يسجل النجاح والفشل والجهاز المعلق والمحظور والحساب المتوقف، مع TTL سبعة أيام.
- إنشاء وتعديل وإيقاف الموظف؛ الإيقاف يتطلب سببًا ويلغي جلساته الحية.
- الموافقة على الجهاز والحظر؛ الحظر يلغي كل Refresh Sessions الخاصة به.
- تغيير مصفوفة الموظف أو صلاحيات الدور يرفع `permissionsVersion`، فتُرفض Access/Refresh tokens القديمة فورًا.
- Role CRUD الأساسي، استبدال صلاحيات الدور، وPermission catalog للقراءة.
- `GET /admin/bootstrap` يعيد الموظف والدور وSidebar permission DTO ومواضع notifications/attendance/shift/realtime بعقد ثابت.
- كل القوائم المنفذة في الشاشة تستخدم Pagination بحد أقصى 10 وترتيبًا ثابتًا.

### الـAPI المنفذة

- `POST /auth/login`, `POST /auth/refresh`, `POST /auth/logout`.
- `GET /admin/bootstrap`.
- `GET /employees-screen`, `POST /employees`.
- `GET /employees/:id`, `PATCH /employees/:id`.
- `POST /employee-devices/:id/approve`, `POST /employee-devices/:id/block`.
- `PUT /employees/:id/permission-matrix`.
- `GET /roles`, `POST /roles`, `PATCH /roles/:id`, `PUT /roles/:id/permissions`.
- `GET /permissions`.

### بوابة تحقق الدفعة 3

- [x] `npm run lint` ناجح.
- [x] `npm test` — 6 ملفات و32 اختبارًا ناجحًا.
- [x] `npm run format:check` ناجح.
- [x] Password لا يظهر بلا الصلاحية المخصصة ويظهر معها.
- [x] جهاز جديد يرجع 202 ولا ينشئ Session كاملة.
- [x] الجهاز الموافق عليه يصدر Access/Refresh ويعيد Sidebar DTO.
- [x] حظر الجهاز يلغي جلساته.
- [x] تغيير permissionsVersion يبطل Access token القديم.
- [x] تعطيل الموظف يمنع Login وRefresh ويحفظ نتيجة المحاولة/الإلغاء.
- [x] Refresh rotation يخزن Hash فقط، ولا يعيد استخدام الجلسة السابقة.
- [x] Unknown fields ترفض قبل الوصول لقاعدة البيانات.

## الدفعة 4 — الحضور والانصراف: VERIFIED

تم تنفيذ AttendanceRecord وAttendanceAdjustment وربطهما بالمصادقة والموظفين:

- تسجيل الحضور تلقائيًا بعد Login ناجح من جهاز APPROVED وفي نفس Transaction.
- حفظ schedule/timezone/grace snapshot وقت الحضور حتى لا تغير تعديلات جدول الموظف التاريخ القديم.
- منع أكثر من سجل OPEN للموظف بفحص الخدمة وpartial unique index للحماية من الـrace.
- Login متكرر يعيد سجل الحضور المفتوح ولا ينشئ سجلًا ثانيًا.
- حساب attendanceDate والتأخير ومدة العمل بتوقيت `Africa/Cairo`، ودعم الوردية العابرة لمنتصف الليل.
- انصراف إداري من صفحة الموظف مع expectedVersion وحساب workedMinutes.
- منع الانصراف وForce Close إذا كان الموظف مسؤولًا عن درج مفتوح عبر Drawer typed port إلزامي.
- Force Close يتطلب سببًا وينشئ Adjustment تاريخيًا.
- التصحيح الإداري يدعم check-in/check-out أو الاثنين، يعيد حساب التاريخ والتأخير والعمل، ويحفظ old/new values والسبب والموظف المنفذ.
- AttendanceAdjustment immutable: يمنع update/delete على مستوى Mongoose hooks.
- Audit وOutbox داخل Transaction لكل إنشاء وإغلاق وتصحيح.
- `admin/bootstrap` يعيد currentAttendance الفعلي.
- القوائم بحد أقصى 10، sort ثابت، وفلاتر employee/status/date range.

### الـAPI المنفذة

- `POST /attendance/check-in`.
- `GET /attendance?page=1&limit=10&employeeId=&status=&from=&to=`.
- `GET /attendance/:id`.
- `POST /attendance/:id/check-out`.
- `POST /attendance/:id/adjustments`.
- `POST /attendance/:id/force-close`.

### بوابة تحقق الدفعة 4

- [x] `npm run lint` ناجح.
- [x] `npm test` — 7 ملفات و38 اختبارًا ناجحًا.
- [x] `npm run format:check` ناجح.
- [x] Cairo timezone وgrace/late calculations.
- [x] Overnight attendanceDate ومدة العمل عبر منتصف الليل.
- [x] Duplicate check-in يعيد السجل نفسه.
- [x] Approved login يعيد currentAttendance.
- [x] Admin checkout يحسب المدة ويغلق السجل.
- [x] درج مفتوح يمنع الانصراف.
- [x] Correction يحفظ old/new values والسبب ولا يغير التاريخ بصمت.

## الدفعة 5 — الموردون والحساب اليدوي: VERIFIED

تم تنفيذ Supplier وSupplierAccount وSupplierAccountEntry وربطها بالصلاحيات وAudit/Outbox. إنشاء المورد ينشئ حسابًا صفريًا في Transaction واحدة. الدين والمستحق منفصلان ويدويان ولا يرتبطان بالمشتريات أو الدرج. دفع الدين Cash OUT وتحصيل المستحق Cash IN عبر Drawer typed port داخل Transaction نفسها.

القيد يحفظ `occurredOn` و`recordedAt/By` والرصيدين بعد الحركة، وهو immutable. المبلغ الأكبر من الرصيد مرفوض والمساوي يغلق الرصيد إلى صفر. المورد المتوقف لا يقبل دينًا أو مستحقًا جديدًا، مع السماح بتسوية وعكس القديم. العكس ينشئ REVERSAL جديدًا، يمنع التكرار وعكس REVERSAL، ويرفض أي عكس يجعل الرصيد سالبًا. عكس التسوية النقدية يعكس حركة الدرج أيضًا.

الشاشة والتفاصيل وسجل القيود تستخدم Pagination 10 وstable sort، وتعيد ملخص active/inactive وإجمالي الدين والمستحق والفلاتر. `include=materials` يستخدم typed port حتى يكتمل موديول المخزون.

### الـAPI المنفذة

- `GET /suppliers-screen`, `POST /suppliers`.
- `GET /suppliers/:id`, `PATCH /suppliers/:id`, `POST /suppliers/:id/status`.
- `POST /suppliers/:id/account-entries`, `GET /suppliers/:id/account-entries`.
- `POST /supplier-account-entries/:id/reverse`.

### بوابة تحقق الدفعة 5

- [x] إنشاء المورد والحساب الصفري ذريًا.
- [x] الدين والمستحق مستقلان ولا يلمسان الدرج.
- [x] الزيادة على الرصيد مرفوضة والتسوية الدقيقة مسموحة.
- [x] المورد المتوقف يمنع الرصيد الجديد ويسمح بتصفية القديم.
- [x] expectedAccountVersion يمنع double-click والـrace من إنتاج أثرين.
- [x] العكس مرة واحدة، مع عكس حركة الدرج عند الحاجة.
- [x] Drawer غير المتاح يفشل مغلقًا بـ503 وتعود Transaction.
- [x] `npm run lint` و`npm run format:check` ناجحان؛ 8 ملفات و51 اختبارًا ناجحًا.

## الدفعة 6 — وحدات القياس والمواد الخام والمخزون: VERIFIED

تم تنفيذ وحدات القياس وربط الوحدة الكبيرة بالصغيرة بمعامل تحويل دقيق، والمواد الخام بمورد إجباري وحدود المخزون والصلاحية. يقفل النظام وحدات المادة بعد أول دفعة، ويعرض شاشة المواد والتفاصيل والدفعات والحركات بقوائم لا تتجاوز 10 عناصر وترتيب ثابت. كما رُبطت مواد المورد بصفحته عبر typed port فعلي.

دفعات المخزون لا تُنشأ إلا من عقد تسجيل المشتريات، وتحفظ الكمية والقيمة الأصلية والمتبقية ولقطات المورد والمادة وتاريخ الاستلام والصلاحية وأولوية السحب. سجل الحركات immutable، والسحب النهائي لا يمكن تشغيله أو استرجاعه. انتهاء الصلاحية لا يمنع البيع، ويظل ظاهرًا للمرحلة التالية الخاصة بالتحذيرات.

المحاكاة تجمع احتياجات المادة قبل الكتابة، ثم توزعها حسب أولوية الدفعات. الاستهلاك الفعلي يحفظ Allocation وتكلفته من الدفعة التي خُصمت فعلًا. الإلغاء يسترجع نفس الكمية والقيمة إلى نفس الدفعة مرة واحدة، ويرفض تجاوز أصل الدفعة. expected versions تمنع بيع وسحب آخر رصيد بالتزامن وتمنع ترتيب أولوية فوق نسخة قديمة. آخر خروج يصفّر الكمية والقيمة تمامًا لتجنب بقايا الكسور.

### الـAPI المنفذة

- `GET /measurement-units`, `POST /measurement-units`, `PATCH /measurement-units/:id`.
- `GET /raw-materials-screen`, `POST /raw-materials`.
- `GET /raw-materials/:id`, `PATCH /raw-materials/:id`.
- `POST /raw-materials/:id/withdrawals`, `PUT /raw-materials/:id/batch-priorities`.
- `GET /raw-material-withdrawals`.

### بوابة تحقق الدفعة 6

- [x] التحويل الدقيق بين الوحدة الكبيرة والصغيرة والتحقق من النوع والخطوة.
- [x] المورد إجباري، وتُقفل الوحدات بعد أول دفعة.
- [x] الخصم يعبر عدة دفعات حسب الأولوية ويسمح بالدفعة منتهية الصلاحية.
- [x] التكلفة الفعلية تأتي من قيمة كل دفعة مستهلكة.
- [x] الاسترجاع يعيد نفس التخصيص مرة واحدة فقط.
- [x] سحب التالف نهائي، وآخر خروج يصفّر الكمية والقيمة.
- [x] optimistic versions تغلق سباق بيع/سحب/ترتيب متزامن.
- [x] كل الحسابات المالية والكميات تستخدم Decimal بلا Number arithmetic.
- [x] `npm run lint` و`npm run format:check` ناجحان؛ 9 ملفات و60 اختبارًا ناجحًا.

## الدفعة 7 — التحذيرات: VERIFIED

تم تنفيذ التحذيرات كمخرجات مشتقة من مصدر حقيقة المخزون، من دون Collection قابلة للتقادم. النظام ينتج تحذير نقص المخزون عند الرصيد الأقل من حد التنبيه أو المساوي له، ويصنف نفاد الرصيد CRITICAL. كما ينتج تحذيرات قرب انتهاء الصلاحية وانتهائها لكل دفعة لها رصيد، وفق تاريخ العمل في `Africa/Cairo` وعدد الأيام المضبوط على المادة.

شاشة التحذيرات تجمع الملخص والعناصر والفلاتر في طلب واحد، وتدعم النوع والمادة والمورد وPagination حتى 10. Header يستخدم Endpoint ملخصًا خفيفًا. كل نتيجة تحمل `evaluatedAt` و`businessToday` و`sourceVersions`. Cache قصير العمر ومفتاحه نسخ المواد والدفعات والرصيد والصلاحية، لذلك تتغير البصمة بعد أي حركة مخزون ولا تُعاد نتيجة قديمة. إذا فشل مصدر بيانات، ترجع `dataQuality:ERROR` و`counts:null` بدل أصفار مضللة.

تم ربط ملخص التحذيرات بشاشة المواد الخام، مع إبقاء انتهاء الصلاحية تحذيرًا فقط وعدم منعه البيع. `openShiftLong` يبقى `null` حتى تنفيذ مصدر تحذيرات الدرج في مرحلته.

### الـAPI المنفذة

- `GET /warnings-screen?type=&materialId=&supplierId=&page=1&limit=10`.
- `GET /warnings/summary`.

### بوابة تحقق الدفعة 7

- [x] حد المخزون المساوي وحد الرصيد صفر.
- [x] أمس منتهي، واليوم وقبل الحد شاملان، واليوم التالي للحد بلا تحذير.
- [x] الدفعة ذات الرصيد صفر لا تنتج تحذير صلاحية.
- [x] الملخص لا يتغير بسبب فلتر قائمة العناصر.
- [x] ترتيب ثابت وPagination بحد أقصى 10.
- [x] Cairo business date ظاهر في العقد.
- [x] فشل المصدر لا يتحول إلى أصفار صحيحة ظاهريًا.
- [x] تغير نسخة/رصيد المخزون يبطل Cache السابق.
- [x] `npm run lint` ناجح؛ 10 ملفات و68 اختبارًا ناجحًا.

## الدفعة 8 — المنتجات والوصفات والكتالوج: VERIFIED

تم تنفيذ الأقسام والمنتجات والأنواع والأحجام والوصفات والإضافات بهيكل مستقل. المنتج يرتبط بقسم نشط، والنوع يرتبط بالمنتج ويحدد المواد المسموحة، والحجم يرتبط بنفس المنتج والنوع ويحفظ سعر البيع Cash/EGP. تعديل الظهور أو الحالة لا يغير Snapshot طلبات سابقة. الصورة تحفظ كـ`imageId` تمهيدًا لـMedia adapter.

استبدال الوصفة يرفض المادة المكررة أو المتوقفة أو غير المسموحة للنوع، ويجلب المواد ووحداتها بكميات مجمعة، ويحفظ اسم المادة والوحدة Snapshot. استخدام المادة في وصفة يقفل المورد والوحدات عليها. التكلفة المتوقعة تحاكي الطلب التالي من دفعات المخزون حسب الأولوية من غير خصم، وتجمع قيمة كل دفعة فعلية، ثم تحسب الربح ونسبة الهامش. نقص المخزون أو غياب الوصفة يظهر في `costCompleteness` ولا ينتج تكلفة وهمية.

الكتالوج العام يعيد الأقسام والمنتجات والأنواع والأحجام والأسعار والإضافات في طلب واحد مع Pagination 10 وETag مبني على نسخ الكتالوج. Projection العميل والباريستا لا يحتوي وصفة أو مواد خام أو دفعات أو تكلفة أو ربح. أضيف Internal snapshot contract للطلبات، وهو الوحيد الذي يعيد وصفة ثابتة للاستهلاك عند تأكيد الطلب. كما رُبطت صفحة المادة بالمنتجات المتأثرة.

### الـAPI المنفذة

- `GET /products-screen`, `POST /products`, `GET /products/:id`, `PATCH /products/:id`.
- `POST /product-categories`, `PATCH /product-categories/:id`.
- `POST /products/:id/types`, `POST /products/:id/sizes`.
- `PUT /product-sizes/:id/recipe`.
- `POST /products/:id/addons`, `PATCH /product-addons/:id`.
- `GET /catalog` عام ويدعم `If-None-Match`/`304`.

### بوابة تحقق الدفعة 8

- [x] تكرار مادة الوصفة مرفوض قبل الكتابة.
- [x] المادة المفقودة أو المتوقفة وغير المسموحة مرفوضة.
- [x] استخدام المادة في الوصفة يقفل المورد والوحدات.
- [x] التكلفة تعبر أكثر من دفعة وتحسب الربح والهامش.
- [x] Catalog version حتمي لنفس نسخ البيانات.
- [x] Public/AI projection لا يسرب Recipe/Cost/Material/Batch/Profit.
- [x] كل القوائم Pagination بحد أقصى 10 وترتيب ثابت.
- [x] `npm run lint` ناجح؛ 11 ملف اختبار و74 اختبارًا ناجحًا.

## الدفعة 9 — المشتريات وفواتير الموردين: VERIFIED

تم تنفيذ فاتورة مشتريات واحدة يمكن أن تحتوي مواد من موردين مختلفين من دون مورد في رأس الفاتورة. عند الإنشاء، يجلب الخادم المادة وموردها الحالي والوحدة الكبيرة والسعر المرجعي، ويحسب الكمية الصغيرة وإجمالي السطر والإجمالي العام باستخدام Decimal، ويحفظ Snapshots للاسم والمورد والوحدة. يمنع تكرار المادة داخل الفاتورة.

يمكن تعديل المسودة أو الفاتورة المقسمة قبل أول تسجيل، ويعاد بناء عناصرها وحساباتها وتصبح بحاجة إلى تقسيم جديد. الحذف Soft Delete ومتاح للمسودة فقط. التقسيم يجمع العناصر حسب المورد المسجل على المادة لحظة إنشاء/تعديل الفاتورة، وينشئ فاتورة مستقلة لكل مورد برقم وإجمالي وحالة، ويمنع stale split وإعادة التقسيم بعد أول تسجيل.

تسجيل العنصر يقبل تاريخ الاستلام والصلاحية، بما في ذلك تاريخ صلاحية منتهٍ أو `null`، وينشئ Batch وInventory Movement داخل Transaction نفسها ثم يربطهما بعنصر الشراء. يحمي `expectedVersion` من التسجيل المكرر. `register-many` يتحقق أولًا أن كل العناصر تنتمي للمجموعة، وبحد 50، ثم يسجلها في Transaction واحدة؛ أي فشل يرجع العملية كلها. حالات العنصر وفاتورة المورد والمجموعة يعاد حسابها إلى غير مسجل أو جزئي أو مسجل بالكامل.

الشاشة تعيد المسودات والمقسمة والمسجلة جزئيًا أو بالكامل بفلتر تبويب وPagination 10. بيانات الطباعة تُبنى من Snapshots التاريخية لكل من الفاتورة المجمعة وفاتورة المورد، ولا تنشئ أثرًا ماليًا على حساب المورد أو الدرج. ملخص التحذيرات يُقرأ بعد Commit حتى يرى الدفعة الجديدة ولا يطيل Transaction.

### الـAPI المنفذة

- `GET /purchases-screen`, `POST /purchase-groups`, `GET /purchase-groups/:id`.
- `PATCH /purchase-groups/:id`, `DELETE /purchase-groups/:id`.
- `POST /purchase-groups/:id/split-by-supplier`.
- `POST /purchase-items/:id/register`.
- `POST /purchase-groups/:id/register-many`.
- `GET /purchase-groups/:id/print-data`.
- `GET /supplier-purchase-invoices/:id/print-data`.

### بوابة تحقق الدفعة 9

- [x] التقسيم الصحيح لمواد عدة موردين وحساب إجمالي كل مورد.
- [x] stale split مرفوض قبل حذف أو إنشاء أي فاتورة مورد.
- [x] الدفعة منتهية الصلاحية مسموح تسجيلها وتظهر للتحذيرات.
- [x] Batch وMovement مرتبطان بعنصر الشراء.
- [x] التسجيل المكرر لا ينشئ Batch ثانية.
- [x] `register-many` يفحص ملكية كل العناصر قبل أول كتابة للمخزون.
- [x] الحالات المجمعة تعكس عدد العناصر المسجلة.
- [x] لا حركة درج ولا قيد حساب مورد من المشتريات.
- [x] `npm run lint` ناجح؛ 12 ملف اختبار و78 اختبارًا ناجحًا.

## الدفعة 10 — مرتجعات المشتريات: VERIFIED

تم تنفيذ فاتورة مرتجع مشتريات نهائية تحتوي رأسًا وعناصر مرتبطة صراحةً بالدفعات. يختار المستخدم المادة عبر دفعتها ثم يحدد الكمية بالوحدة الكبيرة والسبب؛ الخادم يحولها إلى الوحدة الصغيرة، ويتحقق من خطوة المادة والرصيد ونسخة الدفعة، ويحسب قيمة المرتجع من القيمة الحالية لنفس الدفعة. إذا خرج آخر رصيد تُصفّر الكمية والقيمة تمامًا.

الخدمة تقرأ كل الدفعات والمواد وتبني خطة كاملة قبل أول كتابة. تكرار الدفعة، الدفعة المفقودة، نسخة قديمة بسبب بيع متزامن، أو كمية أكبر من الرصيد يرفض العملية كلها. بعد نجاح الخطة تنشئ الرأس، وتخصم كل عنصر كحركة `PURCHASE_RETURN`، ثم تحفظ عناصر immutable ومع كل عنصر `movementId` منذ أول إنشاء. أي فشل داخل التنفيذ يرجع Transaction كاملة.

الفاتورة وعناصرها immutable ولا يوجد تعديل أو حذف أو تشغيل لاحق للكمية المرتجعة. سجل المرتجعات يدعم التاريخ والمورد وPagination 10، والتفاصيل والطباعة مبنيان من Snapshots تاريخية للمادة والدفعة والمورد. العملية لا تستدعي حساب المورد ولا الدرج حسب قرار النظام.

### الـAPI المنفذة

- `GET /purchase-returns-screen?page=1&limit=10&from=&to=&supplierId=`.
- `POST /purchase-returns`.
- `GET /purchase-returns/:id`.
- `GET /purchase-returns/:id/print-data`.

### بوابة تحقق الدفعة 10

- [x] قيمة المرتجع الجزئي من نفس قيمة الدفعة.
- [x] الكمية الأكبر من الرصيد مرفوضة قبل الكتابة.
- [x] expected batch version يغلق سباق البيع والمرتجع.
- [x] الضغط المكرر بالنسخة القديمة لا ينشئ فاتورة ثانية.
- [x] آخر خروج يصفّر كمية وقيمة الدفعة.
- [x] عنصر المرتجع يرتبط بحركة المخزون عند أول إنشاء.
- [x] Models تمنع تعديل أو حذف الفاتورة وعناصرها.
- [x] لا حساب مورد ولا حركة درج.
- [x] `npm run lint` ناجح؛ 13 ملف اختبار و83 اختبارًا ناجحًا.

## الدفعة 11 — الدرج والورديات: VERIFIED

تم تنفيذ فتح وردية واحدة لكل موظف/نطاق، مع رصيد افتتاحي لا يدخل الإيرادات أو المصروفات. كل حركة وارد أو صادر، يدوية أو آلية، تحدث إجماليات الوردية والرصيد المتوقع وتحفظ Ledger immutable متسلسلًا. الصادر يرفض تجاوز النقد المتاح، والعكس متاح للحركات اليدوية مرة واحدة داخل وردية مفتوحة.

الإغلاق يعيد حساب Ledger قبل الحفظ؛ أي اختلاف يمنع الإغلاق وتظل الوردية مفتوحة. يحفظ النظام الرصيد الفعلي، الفرق عن الافتتاحي، فرق المصالحة، وحالة مطابق/عجز/زيادة وقيم العجز والزيادة والسبب. سجل الورديات والحركات والتنبيهات يستخدم Pagination 10، ولكل وردية بيانات طباعة كاملة.

تم تنفيذ تنبيه الوردية الطويلة بموعد أول بعد 12 ساعة، ومفتاح فريد لكل `shiftId + thresholdHours`، مع Outbox لمنع فقد الإشارة أو تكرارها. وتم استبدال Drawer adapters المؤقتة بخدمة فعلية لتسويات الموردين ومنع انصراف موظف لديه درج مفتوح.

### الـAPI المنفذة

- `GET /cash-drawer-screen`.
- `POST /cash-drawer-shifts`, `GET /cash-drawer-shifts`, `GET /cash-drawer-shifts/:id`.
- `POST /cash-drawer-shifts/:id/cash-in`, `POST /cash-drawer-shifts/:id/cash-out`.
- `POST /cash-drawer-transactions/:id/reverse`.
- `POST /cash-drawer-shifts/:id/close`.
- `GET /cash-drawer-shifts/:id/transactions`, `GET /cash-drawer-shifts/:id/alerts`.
- `GET /cash-drawer-shifts/:id/print-data`.

### بوابة تحقق الدفعة 11

- [x] رصيد الافتتاح لا يدخل إجمالي الوارد أو الصادر.
- [x] الرصيد المتوقع يتحدث مع كل حركة.
- [x] الصادر الأكبر من الرصيد مرفوض.
- [x] اختلاف Ledger يمنع الإغلاق وتظل الوردية مفتوحة.
- [x] expectedVersion والفهرس الجزئي يغلقان سباقات الفتح والحركة والإغلاق.
- [x] Supplier cash typed port وAttendance guard مربوطان بالتنفيذ الفعلي.
- [x] `npm run lint` ناجح؛ 14 ملف اختبار و86 اختبارًا ناجحًا.

## الدفعة 12 — الدفع النقدي والفواتير: VERIFIED

تم تنفيذ الدفع النقدي CASH فقط بنمطي DIRECT وCOD، مع الفاتورة النهائية الثابتة:

- تحصيل DIRECT ينشئ دفعة بحالة SETTLED وحركة درج IN واحدة من نوع ORDER_CASH_SALE داخل Transaction نفسها، ويرفض أي مبلغ أكبر من المتبقي على الطلب.
- تحصيل COD يتطلب إسناد توصيل نشطًا، ويسجل الدفعة بحالة COLLECTED كعهدة مندوب من غير حركة درج.
- تسوية COD تحول دفعة COLLECTED واحدة إلى SETTLED مع حركة درج IN واحدة من نوع COD_SETTLEMENT، والدفعة المسواة أو المتغيرة مرفوضة بتعارض نسخة.
- الرد النقدي يرفض أي مبلغ أكبر من المتاح، وينشئ حركة درج OUT واحدة من نوع ORDER_CASH_REFUND، ويحدث إجمالي المردود وحالة الدفعة إلى PARTIALLY_REFUNDED أو REFUNDED.
- عند غياب وردية مفتوحة أو نقص النقد ينشأ الرد بحالة PENDING_CASH_REFUND من غير تغيير إجماليات الدفع، ويُنفذ لاحقًا بحركة OUT واحدة وتحديث الإجماليات.
- الفاتورة النهائية تُنشأ مرة واحدة لكل طلب بحالة READY أو OUT_FOR_DELIVERY أو COMPLETED، والتكرار يعيد نفس الفاتورة من غير إنشاء مكرر. حدث الطباعة يزيد العداد فقط ولا يغير أي إجمالي مالي.
- Checksum حتمي لا يتأثر بترتيب المفاتيح، وpayload الفاتورة يُنظف من الأسرار قبل الحفظ.
- كل العمليات داخل Transaction مع Audit من فئة FINANCIAL وOutbox داخلها، والمبالغ String عشرية بلا Number arithmetic.
- الاعتماد على الطلبات عبر typed ordersPort بنفس نمط الدفعات السابقة: غيابه يفشل مغلقًا بـ503 ORDERS_PORT_UNAVAILABLE، والاختبارات تستخدم mock للمنفذ فقط.
- إصلاح عطل كان يمنع أي معاينة أو إنهاء فاتورة: الخدمة كانت تستورد `redactObject` غير الموجودة، وصُححت إلى `redactSensitive` المستخدمة في كل الكود.

### الـAPI المنفذة

- `POST /orders/:id/payments`, `GET /orders/:id/payments`.
- `POST /order-payments/:id/settle`.
- `POST /order-payments/:id/refunds`.
- `POST /cash-refunds/:id/complete`.
- `GET /invoices`, `GET /invoices/:id`.
- `GET /invoices/:id/print-data`, `POST /invoices/:id/print-events`.
- `GET /orders/:id/invoice-preview`, `POST /orders/:id/invoice-finalize`.

### بوابة تحقق الدفعة 12

- [x] لا دفع غير نقدي على مستوى الـvalidation والخدمة.
- [x] التسوية المكررة أو على دفعة متغيرة مرفوضة.
- [x] نقص الكاش ينتج PENDING_CASH_REFUND من غير تغيير الإجماليات.
- [x] الفاتورة النهائية ثابتة والتكرار يعيد نفس المستند.
- [x] الطباعة عداد فقط ولا تغير المال.
- [x] المبلغ الأكبر من المتبقي أو المتاح مرفوض قبل الكتابة.
- [x] COD من غير إسناد نشط مرفوض.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و14 اختبارًا ناجحًا.

## الدفعة 13 — الطلبات والخصم الفعلي: VERIFIED

تم تنفيذ دورة الطلب الإداري كاملة بتسعير الخادم وخصم المخزون الفعلي:

- التأكيد يسعر من snapshots المنتجات ولا يقبل أي إجمالي من العميل، ويحسب الضريبة ورسم التوصيل من إعدادات العمل، ثم ينشئ الطلب والأصناف ويخصص كل صنف من دفعات المخزون حسب الأولوية داخل Transaction واحدة. نفاد أي خامة يفشل العملية كلها.
- التكلفة الفعلية والربح من قيم الدفعات المستهلكة فعلًا، وcostCompleteness تصبح PARTIAL عند وجود إضافات بلا تكلفة وصفة.
- الإضافة على طلب READY تعيده إلى PREPARING، والإلغاء يعيد نفس التخصيصات مرة واحدة ويحدث الإجماليات والتقدم، وإلغاء آخر صنف نشط يلغي الطلب.
- إتمام التيك أواي يشترط READY، ويحصل المتبقي نقدًا عبر موديول الدفع، وينهي الفاتورة عبر موديول الفواتير، ثم يغلق الطلب COMPLETED داخل نفس السياق.
- إتمام الصالة والتوصيل يتحققان من الحالة ثم يفوضان لمنفذي tablesPort وdeliveryPort، وغيابهما يفشل مغلقًا بـ503.
- الإلغاء لا ينفذ ردًا نقديًا تلقائيًا؛ يعيد المخزون ويقترح refundCase ينفذها الكاشير عبر API الدفع.
- أحداث الحالة للطلب والصنف immutable، وeventSequence يتزايد مع كل حدث، والتقدم مشتق من الأصناف النشطة.
- تم حقن ordersPort الفعلي (getForPayment/applyPaymentSummary/getSummary/getInvoicePayload) فأصبح الدفع والفواتير يعملان على طلبات حقيقية بدل 503.

### الـAPI المنفذة

- `POST /orders`, `GET /orders-online-screen`, `GET /orders/:id`.
- `POST /orders/:id/items`.
- `POST /orders/:id/items/:itemId/cancel`, `POST /orders/:id/cancel`.
- `POST /orders/:id/complete-takeaway`.
- `POST /preparation/order-items/:itemId/ready` (الانتقال فقط؛ شاشات التحضير في الدفعة 14).

### بوابة تحقق الدفعة 13

- [x] التسعير من snapshots الخادم ولا إجمالي من العميل.
- [x] نفاد المخزون يفشل التأكيد كاملًا.
- [x] التكلفة متعددة الدفعات والربح محسوبان من القيم الفعلية.
- [x] الإضافة على READY تعيده PREPARING.
- [x] الإلغاء يعيد نفس التخصيصات مرة واحدة ويقترح refundCase للمدفوع.
- [x] آخر صنف جاهز يحول الطلب READY.
- [x] الإتمام يشترط READY ويربط الدفع والفاتورة.
- [x] سباقات النسخ المتوقعة مغلقة بأكواد تعارض.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و15 اختبارًا ناجحًا.

## الدفعة 14 — شاشات التحضير: VERIFIED

تم تنفيذ شاشات المطبخ فوق انتقال الجاهزية الموجود من الدفعة 13:

- شاشة التحضير تعيد الملخص (current/ready) والكروت بفلتر مجموعة (online/tables) وتبويب (current/ready) بترتيب FIFO الأقدم أولًا وPagination حتى 10.
- كارت التحضير يعرض الأصناف النشطة وحالاتها والتقدم من غير كشف الوصفة الكاملة في القائمة.
- تفاصيل التحضير تعرض كل صنف نشط مع recipeSnapshot المجمدة لحظة التأكيد، فلا تتأثر بتعديل الوصفة لاحقًا.
- أصبح صنف الطلب يحفظ recipeSnapshot (المادة والكمية الصغيرة والاسم والوحدة) عند التأكيد والإضافة.
- الشاشتان للقراءة فقط ولا تغيران أي حالة؛ الانتقال الوحيد يظل `POST /preparation/order-items/:itemId/ready`.

### الـAPI المنفذة

- `GET /preparation/orders?group=&tab=&page=1&limit=10`.
- `GET /preparation/orders/:id`.

### بوابة تحقق الدفعة 14

- [x] فلتر المجموعة والتبويب صحيحان ومجموعة الصالة فارغة قبل جلسات الطاولات.
- [x] الترتيب FIFO والحد الأقصى 10.
- [x] التفاصيل تحمل الوصفة المجمدة ولا تعيد الملغى.
- [x] الطلب المجهول مرفوض بـ404.
- [x] التأكيد يخزن recipeSnapshot للتحضير اللاحق.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و7 اختبارات ناجحة.

## الدفعة 15 — العملاء والتقييمات: VERIFIED

تم تنفيذ العملاء بهاتف موحد والتقييمات بتقييم واحد لكل طلب:

- Upsert العميل بمفتاح `phoneNormalized` مع تطبيع حتمي للأرقام المصرية، وسباق أول طلبين لنفس الهاتف ينتهي بعميل واحد عبر إعادة المحاولة عند duplicate key.
- بيانات الملف (الاسم والعنوان) تتحدث فقط لو تاريخ الطلب أحدث من `lastProfileOrderAt`، والطلب القديم المتأخر لا يمحو الأحدث.
- تأكيد الطلب الإداري يربط `customerId` عبر `customersPort`، والإتمام يسجل `completedOrderCount`، وغياب المنفذ يتجاوز الربط من غير فشل.
- التقييم يُقبل بعد اكتمال الطلب فقط لتوصيل وتيك أواي، وتقييم واحد لكل `orderId` بفهرس فريد، وتقييم الصالة مؤجل لجلسة الطاولة.
- تعديل التقييم يحفظ revision immutable بالقديم والجديد، والإخفاء الإداري بسبب يحفظ الأصل والنجوم.
- تفاصيل العميل تعيد طلباته عبر منفذ الطلبات وتقييماته من سجلاتها، وكلاهما null آمن عند غياب المنفذ.

### الـAPI المنفذة

- `GET /customers-screen`, `POST /customers`.
- `GET /customers/:id`, `PATCH /customers/:id`.
- `GET /reviews`, `POST /orders/:id/reviews`, `GET /orders/:id/reviews`.
- `PATCH /reviews/:id`, `POST /reviews/:id/moderation`.

### بوابة تحقق الدفعة 15

- [x] هاتف واحد لصيغ أرقام مختلفة وطلبات متزامنة.
- [x] ترتيب الملف بالأحدث زمنيًا لا بالأحدث وصولًا.
- [x] الحظر يتطلب سببًا والنسخ المتوقعة مغلقة.
- [x] التقييم بعد الاكتمال فقط وواحد لكل طلب.
- [x] التعديل والإخفاء بسجلات revision ثابتة.
- [x] ربط الطلب بالعميل والإتمام بالعداد.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و11 اختبارًا ناجحًا.

## الدفعة 16 — واجهة العميل العامة: VERIFIED

تم تنفيذ مسارات العميل العام للتيك أواي والتوصيل فوق خط الطلبات الإداري:

- الطلب العام يمر بنفس خط التأكيد الإداري بقناة CUSTOMER_WEB (تسعير الخادم وخصم المخزون)، ثم يصدر توكنين مرة واحدة: قراءة للتتبع وإجراء للإضافة والإلغاء والاستلام والتقييم، ويُحفظ Hash فقط.
- فصل النطاق مطبق: توكن القراءة لا ينفذ إجراءً والعكس، والتوكن المنتهي يُعلم EXPIRED ويُرفض.
- البحث برقم الطلب والهاتف بإسقاط محدود بلا توكنات أو باركود أو إجماليات، والهاتف الخاطئ يرد مثل المجهول تمامًا، مع حد 10 محاولات لكل 10 دقائق لكل IP وطلب.
- طلب الإلغاء العام: غير المدفوع المبكر يُقبل تلقائيًا ويُنفذ فورًا، والمدفوع أو المتقدم يبقى PENDING لمراجعة الإدارة.
- الاستلام يشترط OUT_FOR_DELIVERY مع AVAILABLE ثم يغلق الطلب COMPLETED مع فاتورة نهائية وعلامة تقييم؛ ويعيد `deliveryConfirmation: null` حتى الدفعة 17.
- جلسة العميل تُستبدل من Action Token صالح وتمنح سجل طلباته مع حالة التقييم لكل طلب.
- عقد التخزين المحلي للفرونت: مفتاح `404_customer_profile_v2` يحفظ `{orderNumber, trackingReadToken, orderActionToken}` فقط، ولا يُوثق به الخادم أبدًا، ويُمسح عند انتهاء التوكن أو إلغائه.

### الـAPI المنفذة

- `POST /public-orders`, `POST /public-orders/lookup`.
- `GET /public-orders/:orderNumber/tracking` (هيدر `X-Tracking-Read-Token`).
- `POST /public-orders/:orderNumber/items` (هيدر `X-Order-Action-Token`).
- `POST /public-orders/:orderNumber/cancellation-request` (هيدر `X-Order-Action-Token`).
- `POST /public-orders/:orderNumber/receive` (هيدر `X-Order-Action-Token`).
- `POST /public-orders/:orderNumber/reviews` (هيدر `X-Order-Action-Token`).
- `POST /customer-access-sessions`, `GET /customer/orders` (هيدر `X-Customer-Session`).

### بوابة تحقق الدفعة 16

- [x] التوكن الخام مرة واحدة والمخزن Hash فقط.
- [x] فصل القراءة عن الإجراء والانتهاء المميت.
- [x] إسقاط البحث المحدود وحد الإساءة.
- [x] القبول التلقائي مقابل الانتظار الإداري.
- [x] بوابة الاستلام وحسم الطلب مع الفاتورة.
- [x] الجلسة والسجل مع حالة التقييم.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و13 اختبارًا ناجحًا.

## الدفعة 17 — المناديب والتوصيل: VERIFIED

تم تنفيذ دورة التوصيل من الإسناد حتى التسوية النقدية:

- المندوب بهاتف موحد وحالة وسعة قصوى، والإسناد على طلب READY للتوصيل فقط مع منع الإسناد المزدوج وفهرس نشط فريد لكل طلب.
- التسليم للمندوب يحول الطلب OUT_FOR_DELIVERY مع AVAILABLE، والنقل يغلق الرجل القديمة ويفتح جديدة مع نقل العدادات، والتعذر يبقي الطلب خارجًا، والإعادة للمحل ترجعه READY مع LOCKED.
- تأكيد الاستلام (عميل أو تجاوز إداري بسبب) ينشئ سجل تأكيد بمصدره، ويغلق الطلب COMPLETED مع فاتورة نهائية، ومسار العميل العام أصبح يستخدم نفس الدالة فسجل التأكيد موجود دائمًا.
- تسوية العهدة تجمع مدفوعات COD المعلقة وتسويها بحركات درج IN مرة واحدة مع الرصيد المتبقي، وبلا معلق ترفض صراحة.
- مشاركة واتساب تسجل فتح الرابط فقط ولا تدعي إرسالًا أو تسليمًا.
- الطلب يحمل المندوب والإسناد الحاليين، وقراءة الدفع للتوصيل تعيدهما حقيقيين فمسار COD يعمل end-to-end.

### الـAPI المنفذة

- `GET /delegates-screen`, `POST /delegates`, `GET /delegates/:id`, `PATCH /delegates/:id`.
- `POST /orders/:id/assign-delegate`, `GET /delivery-assignments/:id`.
- `POST /delivery-assignments/:id/handover`, `/reassign`, `/failed`, `/returned`.
- `POST /delivery-assignments/:id/admin-confirm-delivery`.
- `POST /delivery-assignments/:id/settle-cash`.
- `POST /delivery-assignments/:id/whatsapp-share-opened`.

### بوابة تحقق الدفعة 17

- [x] السعة والإسناد المزدوج مغلقان.
- [x] التسليم والنقل والتعذر والإعادة بحالاتها الصحيحة.
- [x] التأكيد بسجل مصدر والفاتورة والعدادات.
- [x] التجاوز الإداري يتطلب سببًا.
- [x] التسوية مرة واحدة للمعلق فقط مع المتبقي.
- [x] الواتساب تسجيل فتح فقط.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و16 اختبارًا ناجحًا.

## الدفعة 18 — الطاولات والجلسات: VERIFIED

تم تنفيذ دورة الصالة من اللوحة حتى الإغلاق والفاتورة:

- مجموعة ثابتة من 20 طاولة تُزرع تلقائيًا عند أول قراءة للوحة، ولوحة من 20 كارت باشتقاق EMPTY/OCCUPIED/OUT_OF_SERVICE مع ملخص وتقدم الطلب النشط.
- أول طلب إداري يفتح جلسة OPEN وطلب DINE_IN بقناة TABLE داخل Transaction واحدة، والفتح الثاني يرفض باحتلال الطاولة حتى مع السباق.
- طلب الصالة بلا عميل مسجل: لقطة ضيف ثابتة ولا upsert في سجلات العملاء.
- الإضافة على الجلسة المفتوحة فقط، والإلغاء يلغي الطلب والجلسة معًا، والإغلاق يشترط READY ويحصل المتبقي نقدًا وينهي الفاتورة ويغلق الطلب والجلسة معًا فتتحرر الطاولة.
- بيانات الطباعة لقطة من الجلسة والطلب ولا تغير أي حالة.
- منفذ `tablesPort.closeSession` الفعلي مربوط في `v1.routes` فإتمام الصالة من خط الطلبات يعمل بدل 503.

### الـAPI المنفذة

- `GET /tables-board`, `GET /tables/:id`, `PATCH /tables/:id`.
- `POST /tables/:id/admin-orders`.
- `GET /table-sessions/:id`, `POST /table-sessions/:id/items`.
- `POST /table-sessions/:id/cancel`, `POST /table-sessions/:id/close`.
- `GET /table-sessions/:id/print-data`.

### بوابة تحقق الدفعة 18

- [x] زرع العشرين واشتقاق الإشغال والملخص.
- [x] الفتح الذري ورفض الاحتلال المزدوج.
- [x] خارج الخدمة تمنع الفتح بسبب مسجل.
- [x] الإضافة والإلغاء والإغلاق بحالاتها الصحيحة.
- [x] الإغلاق يحصل ويفوتر ويحرر الطاولة.
- [x] طلب الصالة بلا ربط عميل.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و14 اختبارًا ناجحًا.

## الدفعة 19 — تجربة عميل الطاولة: VERIFIED

تم تنفيذ مسار ضيف الطاولة من مسح QR حتى تأكيد الموظف والتقييم:

- التدوير الإداري لسر QR يبطل جلسات الضيوف النشطة، والتشغيل يتحقق من السر المخزن Hash فقط ويرجع توكن ضيف لمرة واحدة بهيدر `X-Table-Token` وصلاحية 12 ساعة.
- مقترح الضيف لقطة أسعار من الكتالوج بلا طلب ولا خصم مخزون، ومقترح نشط واحد لكل جلسة، وإلغاء الضيف للمنتظر فقط.
- مراجعة الموظف (بدء/طلب تعديل/رفض) بحراسة النسخ، والتأكيد يلحق بطلب الجلسة المفتوحة أو يفتح جلسة وطلبًا جديدين، ويربط الطلب بجلسة الضيف.
- تقييم الصالة من جلسة الضيف المرتبطة فقط بعد اكتمال طلب DINE_IN، بمالك ضيف يمنع التكرار مع أي مالك عبر فهرس الطلب الفريد.
- أثناء التنفيذ اكتُشف وأُصلح عطل حجب `owner` داخل مساعد التقييمات كان يسقط أي تقديم.

### الـAPI المنفذة

- `POST /table-experience/bootstrap`, `POST /table-experience/order-proposals`.
- `GET /table-experience/order-proposals/current`, `POST /table-experience/order-proposals/cancel`.
- `POST /table-experience/orders/:id/reviews` (هيدر `X-Table-Token`).
- `POST /tables/:id/rotate-qr`.
- `GET /table-order-proposals`, `GET /table-order-proposals/:id`.
- `POST /table-order-proposals/:id/start-review`, `/request-changes`, `/reject`, `/confirm`.

### بوابة تحقق الدفعة 19

- [x] QR الخاطئ مرفوض والتوكن Hash فقط لمرة واحدة.
- [x] التدوير يبطل الجلسات النشطة.
- [x] المقترح لقطة بلا أثر طلب/مخزون وواحد نشط فقط.
- [x] التأكيد يلحق أو يفتح ويربط الجلسة.
- [x] التقييم من الجلسة المرتبطة فقط وبمالك واحد.
- [x] عزل الجلسات بالتوكن.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و14 اختبارًا ناجحًا.

## الدفعة 20 — خدمات الطاولة: VERIFIED

تم تنفيذ طلبات خدمة الصالة الخمسة وربطها بالمقترحات والجلسات:

- إنشاء الضيف بمفتاح مالك SESSION للجلسة التشغيلية أو GUEST قبلها، مع منع مكرر OPEN لكل مالك ونوع وإعادة الموجود بدل التكرار.
- طلب الحساب يشترط طلبًا نشطًا، وتقارير المشاكل عالية الأولوية وتتطلب وصفًا، ولا يخصم أي طلب مخزونًا ولا يحرك درجًا.
- المقترح المرسل يفتح تلقائيًا نداء مراجعة مرتبطًا به، وتأكيد المقترح يحل النداءات المفتوحة عليه.
- إغلاق أو إلغاء الجلسة يحل طلب الحساب المفتوح ويلغي الباقي تلقائيًا.
- شاشة الإدارة بملخص المفتوح وعالي الأولوية ومتوسط الاستجابة وقائمتين للمنجز والمفتوح.

### الـAPI المنفذة

- `POST /table-experience/services`, `POST /table-experience/services/:id/cancel`.
- `GET /table-services-screen`, `GET /table-service-requests/:id`.
- `POST /table-service-requests/:id/resolve`.

### بوابة تحقق الدفعة 20

- [x] مفتاح المالك حسب الجلسة ومنع المكرر.
- [x] شروط الحساب والأولوية بلا أثر مخزون/درج.
- [x] نداء المراجعة التلقائي وحله عند التأكيد.
- [x] تسوية الإغلاق للحساب والباقي.
- [x] الشاشة بالملخص والقائمتين.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و12 اختبارًا ناجحًا.

## الدفعة 21 — حالات الطلبات واسترداد المرتجعات: VERIFIED

تم تنفيذ آلة حالات طلبات الإلغاء العامة واسترداد المرتجعات المعلقة:

- الموافقة الإدارية تنفذ إلغاء الطلب المعلق بنفس سببه وتغلقه EXECUTED مع حالة الرد المقترحة، وطلبات خارج التوصيل تُرد للمحل أولًا بدل تنفيذها.
- الرفض الإداري بسبب يغلق الطلب REJECTED ويبقي طلب العميل ساريًا.
- إعادة المحاولة تكمل الرد المعلق عند توفر النقد، وتبقيه معلقًا بلا استثناء عند استمرار العجز، والمسح الدوري يمر على أول 50 معلقًا بتقرير المحاولات.
- إلغاء أي طلب (كامل أو بآخر صنف) يغلق إسناداته النشطة تلقائيًا بحالة CANCELLED مع تحرير عداد المندوب.
- شاشة الحالات بملخص المعلق والقوائم حتى 10 عناصر.

### الـAPI المنفذة

- `GET /order-cancellation-requests`, `GET /order-cancellation-requests/:id`.
- `POST /order-cancellation-requests/:id/approve`, `/reject`.
- `POST /cash-refunds/:id/retry`, `POST /cash-refunds/sweep`.

### بوابة تحقق الدفعة 21

- [x] الموافقة تنفذ والرفض يغلق بالسبب.
- [x] طلبات خارج التوصيل تُرد للمحل أولًا.
- [x] الاسترداد يكمل أو يبقي معلقًا بلا كسر.
- [x] المسح بتقرير المحاولات والنواقص.
- [x] الإلغاء يغلق الإسناد النشط تلقائيًا.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و9 اختبارات ناجحة.

## الدفعة 22 — لوحة القيادة والمزامنة اللحظية: VERIFIED

تم تنفيذ شاشة قيادة يومية ومزامنة أحداث بصلاحيات:

- الداشبورد يعيد ملخص الطلبات والمبيعات والتحضير والطاولات والخدمات والتوصيل والدرج في طلب واحد بتاريخ عمل القاهرة، مع مقارنة الأمس ومؤشر cursor اللحظي.
- الإسقاط اليومي يُبنى من نماذج القراءة ويُعاد بناؤه عند القدم (60 ثانية)، وفشل أي مصدر يرفع `dataQuality:ERROR` بمصادره بدل أصفار مضللة.
- المزامنة تقرأ من Outbox (مصدر الحقيقة للتعافي) بفلتر الغرف المطلوبة والمسموحة وحد أقصى 100 حدث مع `hasMore` ولقطات الحالة الحية للطلبات والجلسات.
- الغرف الإدارية تُفلتر بصلاحيات الموظف، وغرف الطلب/الطاولة/الضيف/العميل تُفتح بتوكناتها فقط، وبلا اعتماد تُرفض بـ401.
- النشر اللحظي يوزع المغلف الموحد على المشتركين المسجلين؛ نقل Socket.IO الحي مؤجل والواجهة جاهزة له.

### الـAPI المنفذة

- `GET /dashboard-screen`, `GET /realtime/sync?rooms=&afterSequence=`.

### بوابة تحقق الدفعة 22

- [x] تجميع بلا N+1 وحدود صفحات ثابتة.
- [x] إعادة البناء والمقارنة ومؤشر التقدم.
- [x] فلترة الصلاحيات للغرف الإدارية والمنع للعامة خارج نطاقها.
- [x] التكرار والفجوات آمنة (قراءة idempotent بحد).
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و15 اختبارًا ناجحًا.

## الدفعة 23 — التقارير المالية والتدقيق: VERIFIED

تم تنفيذ تقارير القراءة فقط وسجل التدقيق وصندوق الإشعارات:

- شاشة التقارير تجمع المبيعات والتكلفة والربح والنقدية والمخزون والمديونية والعهد في طلب واحد بمدى قاهرة ومقارنة فترة سابقة، مع اتجاه يومي ومزيج قنوات وأعلى منتجات وتنبيهات.
- خمسة تقارير تفصيلية بملخص وعناصر مرقمة وتفصيلات، وكاش 5 دقائق بمفتاح مستقر، وفشل أي مصدر يnull حقوله بدل أصفار مضللة.
- التصدير 202 بصف معالجة فورية وتشيك sum مستقر وحد 500 صف، وملفات التخزين الخارجي مؤجلة (الحمولة مضمنة).
- سجل التدقيق بشاشة ملخصات وفلاتر وتفاصيل كاملة وtimeline لكل كيان، وتصديره على نفس بنية التقارير.
- الإشعارات بمفتاح منع تكرار وقراءة فردية وجماعية وعداد غير المقروء، والإنشاء عبر منفذ للدفعات القادمة.
- أثناء التنفيذ اكتُشف وأُصلح: اتجاه المبيعات كان يقرأ حقلًا غير موجود، والتواريخ الوهمية كانت تعبر الـ validation.

### الـAPI المنفذة

- `GET /financial-reports-screen`, `/financial-reports/sales|inventory|drawer|suppliers|delegates`.
- `POST /financial-reports/exports`, `GET /financial-reports/exports/:id`.
- `GET /audit-events-screen`, `GET /audit-events/:id`, `GET /entities/:entityType/:entityId/timeline`.
- `POST /audit-events/exports`, `GET /audit-events/exports/:id`.
- `GET /notifications`, `POST /notifications/:id/read`, `POST /notifications/read-all`.

### بوابة تحقق الدفعة 23

- [x] البطاقات من كل المصادر دفعة واحدة.
- [x] المصادر الفاشلة null معلنة لا أصفار.
- [x] الكاش يمنع إعادة الاستعلام.
- [x] التصدير 202 بحمولة وتشيك مستقرين.
- [x] التدقيق بملخصات وtimeline ومنع التكرار في الإشعارات.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و12 اختبارًا ناجحًا.

## الدفعة 24 — الباريستا الذكي وربط الفرونت: VERIFIED

تم تنفيذ مساعد المينيو العام وربط محادثة الفرونت به:

- الباريستا يرى الإسقاط العام للكتالوج فقط (أسماء وأسعار وتوافر)، والـ prompt ثابت بلا بيانات عميل، وأي ID خارج الكتالوج يُسقط قبل الرد.
- غياب المفتاح أو تعطل المزود يتحول لبديل محلي من الكتالوج نفسه بنفس العقد، والمهلة 8 ثوان بحد 20 رسالة/دقيقة لكل IP.
- إجراءات السلة المقترحة خاملة (لا تمس سلة أو طلبًا) والفرونت يعيد التحقق منها قبل العرض.
- الفرونت: `chatService` يتكلم عقد v2 الجديد بنفس شكل الإرجاع القديم (`reply`) فلا تغيير في الشاشات، والمسار أصبح `/v1/customer-ai/chat`.

### الـAPI المنفذة

- `POST /customer-ai/chat` (عام بلا مصادقة).

### بوابة تحقق الدفعة 24

- [x] حدود الإدخال وحارس الأدوات.
- [x] إسقاط المجهول والحفاظ على الكتالوج فقط.
- [x] البديل المحلي عند غياب/تعطل المزود.
- [x] حد الإساءة لكل عميل.
- [x] توافق شاشتي المحادثة بلا تعديل فيهما.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و7 اختبارات ناجحة.

## الدفعة 25 — التثبيت والتشغيل: VERIFIED

تم تنفيذ بوابة الإطلاق: فهارس وترحيلات وseed وفحص عقود ومسح أمان:

- بيان الفهارس يُقرأ من 60+ موديل والفرق يمنع الناقص والمختلف ويحذر من الزائد بلا حذف تلقائي (`indexes:check` و`indexes:apply`).
- عداء الترحيلات بقفل إيجار وتشيك sum يمنع الانحراف (`migrate` و`migrate:status`)، ولا ترحيلات معلقة على سكيما جديدة.
- الزرع المرجعي idempotent (5 وحدات + كتالوج الصلاحيات كاملًا بالتسميات + 20 طاولة)، وإنشاء أول أدمن يرفض التكرار ولا يطبع كلمة المرور.
- فحص السلامة read-only (أرصدة الطلبات وروابط الإسناد والجلسات والتخصيصات وصحة Outbox) بخروج غير صفري عند الفشل.
- فحص العقود يمشي 187 مسارًا فعليًا: المانيفست المثبت يجبر التحديث الواعي، وكل مسار بحارس ومحقق ومعالج، وكل طفرة بتحقق.
- المسح الأمني نظيف: بلا TODOs ولا console ولا أسرار مزروعة ولا أنماط خطرة، وhelmet/cors/حدود JSON مفعلة.
- أثناء التنفيذ اكتُشف وأُصلح عطل حرج: استيراد مفقود كان يمنع إقلاع التطبيق في Node الحقيقي (الاختبارات كانت متساهلة)، ومساران بلا تحقق، وقفل ترحيل يسرق الحيازة.

### الأوامر الجديدة

- `npm run migrate`, `migrate:status`, `seed`, `seed:admin`, `indexes:check`, `indexes:apply`, `integrity:check`.

### بوابة تحقق الدفعة 25

- [x] بيان الفهارس والفرق والمنع والحذر.
- [x] القفل والإيجار والترتيب وانحراف التشيك.
- [x] تسمية كل الصلاحيات والزرع المتكرر الآمن.
- [x] رفض الأدمن الثاني وعدم طباعة السر.
- [x] فحوص السلامة الخمسة بتقرير JSON.
- [x] عقد 187 مسارًا بلا مخالفة.
- [x] `npm run lint` ناجح؛ ملفا اختبار و16 اختبارًا ناجحًا.

### قائمة الإطلاق (تشغيل يدوي موثق)

1. `npm run migrate` على Staging ثم `migrate:status` للتأكيد.
2. `npm run indexes:check` (أو `indexes:apply` لأول تثبيت) والمنع عند الانحراف.
3. `npm run seed` ثم `seed:admin --name= --password=` لمرة واحدة.
4. `npm run integrity:check` كخط أساس، و`npm test` و`test:contract` خضراوان.
5. مراقبة أول 24 ساعة: الأخطاء والزمن والاستعلامات البطيئة وOutbox والفهارس.

## الدفعة 26 — الوسائط والنقل الحي والعمال: VERIFIED

تم تنفيذ رفع الصور والبث اللحظي والعمال الخلفيين:

- الوسائط: رفع صور (2MB بفحص بصمة jpeg/png/webp وتشيك sum) بتخزين محلي مُتجاهل git، وروابط موقعة HMAC بربع ساعة، وحذف محمي من الاستخدام، وربط المنتجات يتحقق من الجاهزية عند التمرير فقط.
- النقل الحي: مصادقة Socket بنفس عقد HTTP (جلسة وجهاز ونسخة صلاحيات)، والاشتراك مفلتر بالغرف المسموحة، والبث من سجل المشتركين لنفس المغلف الموحد.
- العمال: ناشر Outbox (نشر/إعادة/دفن بعد 10 محاولات) + تنبيهات الوردية بإشعارات مانعة التكرار + صادرات معلقة، كلها بتيك محقونة قابلة للاختبار وتعمل من `server.js` عند التفعيل.
- أثناء التنفيذ اكتُشف وأُصلح: استثناءات lint حقيقية للسكريبتات وglobals الناقصة.

### الـAPI المنفذة

- `POST /media/uploads`, `GET /media`, `GET /media/:id`, `GET /media/:id/content`, `DELETE /media/:id`.

### بوابة تحقق الدفعة 26

- [x] رفض الفارغ والزائد والمزيف بلا تخزين.
- [x] التوقيع المنتهي والمزور مرفوضان.
- [x] الحذف المحمي والربط المشروط.
- [x] رفض السوكت بلا توكن والاشتراك المفلتر والبث للغرف.
- [x] النشر والدفن وإعادة المحاولة والإشعارات والصادرات.
- [x] `npm run lint` ناجح؛ ملف اختبار واحد و13 اختبارًا ناجحًا.

## الدفعة 27 — ربط الفرونت بعقد v1: VERIFIED

تم تنفيذ حزمة الربط في طبقة services فقط بلا كسر القديم:

- قسم `v1` في المسارات (bootstrap/catalog/public/table/dashboard/sync) بجانب القديم، والمسارات القديمة لم تُمس.
- الكتالوج مزدوج الشكل يقبل v2 (أصناف متداخلة وفئات بأسماء) وينتج نفس شكل عناصر القائمة.
- دوال عامة جديدة للطلبات (إنشاء/تتبع بالهيدر/بحث/إضافة/إلغاء/استلام) وللضيف (تشغيل/مقترح/خدمات/تقييم) بهيدرات v2 الصحيحة.
- مزامنة ما بعد إعادة الاتصال ودالة bootstrap بنفس شكل الجلسة القديمة للتبني الاختياري.
- الشاشات والمكونات لم تتغير؛ التبني شاشة بشاشة في الدفعة القادمة.

### الملفات المعدلة (فرونت)

- `services/endpoints.js` و`catalogService.js` و`realtime.js` و`chatService.js` (سابقًا).
- `modules/customer/checkout/services/orderGateway.js` و`modules/table/services/tableGateway.js`.
- `modules/auth/services/authService.js` (دالة `bootstrapV1` فقط).

### بوابة تحقق الدفعة 27

- [x] المسارات القديمة untouched والدوال القديمة untouched.
- [x] صياغة كل الملفات المعدلة سليمة (`node --check`).
- [x] الهيدرات مطابقة للعقد (X-Tracking-Read-Token وX-Order-Action-Token وX-Table-Token).
- [x] `npm run lint` و`format:check` و`npm test` للباك إند خضراء (بلا تغيير كود فيه).

## الدفعة 28 — التحقق التكاملي على Atlas: VERIFIED

تم تشغيل البوابات المؤجلة على Replica Set حقيقي (Atlas) لأول مرة:

- الاتصال ناجح والترحيلات بلا معلق ولا مطبق (سكيما جديدة).
- الفهارس: 166 ناقصة طُبقت، والفحص الثاني سليم تمامًا (231 فهرسًا إجمالًا).
- الزرع المرجعي: 5 وحدات + 68 صلاحية + 20 طاولة على `cafe_erp`.
- السلامة: بلا إخفاقات ولا تحذيرات.
- أول أدمن منشأ بدور Admin كامل الصلاحيات (30 صلاحية) وجهاز معتمد.
- دخان حي على السيرفر: health وreadiness وversion ودخول فعلي وbootstrap وداشبورد — كلها ناجحة.
- تكامل حي end-to-end على قاعدة تجريبية منفصلة (مُسحت بعدها): مورد → مادة → مشتريات → دفعة 10000g → منتج بوصفة → طلب مؤكد بخصم 40g وتكلفة فعلية 10.00 → إلغاء باستعادة كاملة — 11/11 ناجحة.
- أثناء التنفيذ اكتُشف وأُصلح Bug حرج لم تمسكه اختبارات الوحدة: حركة الاسترجاع كانت بلا تاريخ (`occurredOn`) فتفشل الكتابة الحقيقية — الآن افتراضي تاريخ اليوم + اختبار انحدار.
- وأُصلح Bug أخطر مسكه الدخان الحي فقط: `redactSensitive` كان يحول أي `Date` إلى `{}` في سجل التدقيق فيسقط الدخول بالكامل — الآن تُحفظ القيم غير العادية كما هي + اختبار انحدار.

## قاعدة تحديث الملف

بعد كل دفعة يحدث هذا الملف بنتائج الأوامر والاختبارات الفعلية. لا تستخدم كلمة VERIFIED بناءً على وجود ملفات فقط.

## مراجعة الأداء الشاملة — 2026-09-13

- أزيل N+1 من لوحة الطاولات وأصبحت قراءات bulk ثابتة.
- dashboard stale يعاد بناؤه في الخلفية مع deduplication بدل حجز طلب المستخدم.
- cache الصلاحيات مربوط بـ`permissionsVersion`، وbootstrap يعيد استخدام snapshot المصادقة.
- deadline التشغيلي 3 ثوانٍ مع SLO متوسط لا يتجاوز ثانية، وإعادة محاولة واحدة في الفرونت.
- أضيفت headers قياس وسجل للمسارات فوق 500ms، ومنع double response بعد timeout.
- فحوص Atlas: الفهارس سليمة، integrity سليمة، لا migrations معلقة ولا dead outbox.

# Contract remediation audit (phases 0-7)

The implementation was rechecked against the `final-2` contracts. Confirmed gaps were fixed: flat list metadata and HTTP 400 validation errors; transaction-required audit writes; pending employee-device listing; employee attendance/activity includes; HR audit/outbox coverage; supplier/unit lock reason and timestamp for first batches and recipe use; schema-level batch balance bounds; chronological last-purchase pricing; live 12-hour drawer warnings; and public module ports at the v1 composition boundary.

Mongoose's optimistic `version` remains zero-based. This is intentional because every `expectedVersion` write contract and existing client/test uses the stored Mongoose value directly; changing only its display origin would create false conflicts.

## مراجعة المطابقة النهائية — 2026-09-12

أعيدت مراجعة الموديولات والمسارات مقابل `final-2`. أضيفت شاشة سجل الطلبات الموحدة وبيانات طباعة الطلب وشاشة التحضير المجمعة، وأزيل N+1 من شاشتي الطلبات والتحضير، ووُصلت تفاصيل الطلب فعليًا بملخص الدفع والفاتورة وإسناد التوصيل. أصبحت شاشة الطلبات الأونلاين تشمل TAKEAWAY وDELIVERY من قنوات الإدارة والعميل.

أزيل استخدام `Number` من تجميع وفرز الأموال في التوصيل والتقارير، وفُعل `maxTimeMS` لاستعلامات Mongo أقل من حد استجابة HTTP البالغ ثانية، وأضيف response deadline، وصُححت وثيقة القيود القديمة المتعارضة مع تحقق Atlas والوسائط والربط.
